<?php
class SingleElimimationTestCase extends TourneyWebTestCase {
  protected $controller = 'SingleEliminationController';
  
  /**
   * Test info.
   */
  public static function getInfo() {
    return array(
      'name' => t('Single Elimination Tournament'),
      'description' => t('Steps through setting up a single elimination tournament and playing all the way through.'),
      'group' => t('Tourney'),
    );
  }
  
  /**
   * Testing 4 contestants
   */
  public function testSingleElim4() {
    // Create the tournament.
    $tournament_name = $this->createTourney($this->controller, 4);
        
    // Set contestants of the First Match as same user.
    $edit = array();
    // Match 1 Slot 1
    $edit['contestant1'] = 'user.3';
    // Match 1 Slot 2
    $edit['contestant2'] = 'user.3';
    // Save Match 1
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
    // Confirm Tourney Module can't set the same user twice.
    $this->assertRaw(t('Contestant 1 and Contestant 2 cannot be the same person'));
    
    // Now change contestant 2 to a different user.
    // Match 1 Slot 2.
    $edit['contestant2'] = 'user.6';
    // Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    // Make sure the link to Tourament is present.
    $this->assertLink(t('Go to tournament !tournament', array('!tournament' => $tournament_name)));
    
    // Play First Match, 'contestant1' (Slot 1 or 'user.3') wins in match 1.
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    
    // Set contestants Match 2
    $edit = array();
    $edit['contestant1'] = 'user.4';
    $edit['contestant2'] = 'user.5';
    // Save Match 2 (/tourney/match/2)
    $this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
    // Make sure Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Second Match
    $edit = array();
    $this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');
    
    // Play Third Match
    $edit = array();
    $this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner users of previous matches are being passed into this game correctly
    $this->assertRaw('<tr class="odd"><td>user.3</td>');
    $this->assertRaw('<tr class="even"><td>user.5</td>');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    
    // Checking 4 man Tourney Winner is displaying on the Tournament page (Tournament 1)
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');
    // End of 4 Man Tourney
    $this->pass("End of 4 Man Single Elimination Tournament ");
   }
  
  /**
   * Testing 5 contestants
   */
  public function testSingleElim5() {
    // Create the tournament.
    $this->createTourney($this->controller, 5);
    
    // Set contestants of the First Match as same user.
    $edit = array();
    // Match 1 Slot 1
    $edit['contestant1'] = 'user.6';
    // Match 1 Slot 2
    $edit['contestant2'] = 'user.7';
    // Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');
    
    // Match 2
    $edit = array();
    $this->drupalGet('tourney/match/2');
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
    // Match 2 Slot 1
    $edit['contestant1'] = 'user.3';
    // Save Match 2 (/tourney/match/2)
    $this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Second Match
    $edit = array();
    $this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');
    
    // Match 3
    $edit = array();
    // Match 3 Slot 1
    $edit['contestant1'] = 'user.4';
    // Match 3 Slot 2
    $edit['contestant2'] = 'user.5';
    // Save Match 3 (/tourney/match/3)
    $this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Third Match
    $edit = array();
    $this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');
    
    // Play Fourth Match
    $edit = array();
    $this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner users of previous matches are being passed into this match correctly
    $this->assertRaw('<tr class="odd"><td>user.6</td>');
    $this->assertRaw('<tr class="even"><td>user.5</td>');
    
    // Checking 5 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');
    // End of 5 Man Tourney
    $this->pass("End of 5 Man Single Elimination Tournament ");
   }
  
  /**
   * Testing 6 contestants
   */
  public function testSingleElim6() {
    // Create the tournament.
    $this->createTourney($this->controller, 6);
    
    $edit = array();
    // Match 1 Slot 1
    $edit['contestant1'] = 'user.6';
    // Match 1 Slot 2.
    $edit['contestant2'] = 'user.7';
    // Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');
    
    // Match 2
    $edit = array();
    $edit['contestant1'] = 'user.5';
    $edit['contestant2'] = 'user.8';
    $this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Second Match
    $edit = array();
    $this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');
    
    // Match 3
    $edit = array();
    $edit['contestant1'] = 'user.3';
    $this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Third Match
    $edit = array();
    $this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check slot 2 winner, same winner as the winner from tourney/match/1
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    
    // Match 4
    $edit = array();
    // Match 4 Slot 1
    $edit['contestant1'] = 'user.4';
    $this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Fourth Match
    $edit = array();
    $this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page. 
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');    

    // Match 5
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/5');
    $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.8</td>'));
    
    // Play Fifth Match
    $edit = array();
    $this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');
    
    // Checking 6 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');
    // End of 6 Man Tourney
    $this->pass("End of 6 Man Single Elimination Tournament ");
   }

  /**
   * Testing 7 contestants
   */
  public function testSingleElim7() {
    // Create the tournament.
    $this->createTourney($this->controller, 7);
    
    $edit = array();
    // Match 1 Slot 1
    $edit['contestant1'] = 'user.6';
    // Match 1 Slot 2.
    $edit['contestant2'] = 'user.7';
    // Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.7</div>');
    
    // Match 2
    $edit = array();
    $edit['contestant1'] = 'user.4';
    $edit['contestant2'] = 'user.9';
    $this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Second Match
    $edit = array();
    $this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');    

    // Match 3
    $edit = array();
    $edit['contestant1'] = 'user.5';
    $edit['contestant2'] = 'user.8';
    $this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Third Match
    $edit = array();
    $this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');    

    // Match 4
    $edit = array();
    // Match 4 Slot 1
    $edit['contestant1'] = 'user.3';
    $this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Fourth Match
    $edit = array();
    $this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check slot 2 winner, same winner as the winner from tourney/match/1
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    
    // Match 5
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/5');
    $this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.8</td>'));
    
    // Play Fifth Match
    $edit = array();
    $this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');
    
    // Match 6
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/6');
    $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.8</td>'));
    
    // Play Sixth Match
    $edit = array();
    $this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    
    // Checking 7 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    // End of 7 Man Tourney
    $this->pass("End of 7 Man Single Elimination Tournament ");
   }
  
  /**
   * Testing 8 contestants
   */
  public function testSingleElim8() {
    // Create the tournament.
    $this->createTourney($this->controller, 8);
    
    $edit = array();
    // Match 1 Slot 1
    $edit['contestant1'] = 'user.3';
    // Match 1 Slot 2.
    $edit['contestant2'] = 'user.10';
    // Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    
    // Match 2
    $edit = array();
    $edit['contestant1'] = 'user.6';
    $edit['contestant2'] = 'user.7';
    $this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Second Match
    $edit = array();
    $this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.7</div>');
    
    // Match 3
    $edit = array();
    $edit['contestant1'] = 'user.4';
    $edit['contestant2'] = 'user.9';
    $this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Third Match
    $edit = array();
    $this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

    // Match 4
    $edit = array();
    $edit['contestant1'] = 'user.5';
    $edit['contestant2'] = 'user.8';
    $this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Fourth Match
    $edit = array();
    $this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');
    
    // Match 5
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/5');
    $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.7</td>'));
    
    // Play Fifth Match
    $edit = array();
    $this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    
    // Match 6
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/6');
    $this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.8</td>'));
    
    // Play Sixth Match
    $edit = array();
    $this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');
    

    // Match 7
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/7');
    $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.8</td>'));
    
    // Play Seventh Match
    $edit = array();
    $this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    
    // Checking 8 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    // End of 8 Man Tourney
    $this->pass("End of 8 Man Single Elimination Tournament ");
   }
  
  /**
   * Testing 9 contestants
   */
  public function testSingleElim9() {
    // Create the tournament.
    $this->createTourney($this->controller, 9);
    
    $edit = array();
    // Match 1 Slot 1
    $edit['contestant1'] = 'user.10';
    // Match 1 Slot 2.
    $edit['contestant2'] = 'user.11';
    // Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.11</div>');
    
    // Match 2
    $edit = array();
    $edit['contestant1'] = 'user.3';
    $this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Second Match
    $edit = array();
    $this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    
    // Match 3
    $edit = array();
    $edit['contestant1'] = 'user.6';
    $edit['contestant2'] = 'user.7';
    $this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Third Match
    $edit = array();
    $this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.7</div>');

    // Match 4
    $edit = array();
    $edit['contestant1'] = 'user.4';
    $edit['contestant2'] = 'user.9';
    $this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Fourth Match
    $edit = array();
    $this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');
    
    // Match 5
    $edit = array();
    $edit['contestant1'] = 'user.5';
    $edit['contestant2'] = 'user.8';
    $this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Fifth Match
    $edit = array();
    $this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');
    
    // Match 6
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/6');
    $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.7</td>'));
    
    // Play Sixth Match
    $edit = array();
    $this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    
    // Match 7
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/7');
    $this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.8</td>'));
    
    // Play Seventh Match
    $edit = array();
    $this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');

    // Match 8
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/8');
    $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.8</td>'));
    
    // Play Eighth Match
    $edit = array();
    $this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    
    // Checking 9 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    // End of 9 Man Tourney
    $this->pass("End of 9 Man Single Elimination Tournament ");
   }

  /**
   * Testing 10 contestants
   */
  public function testSingleElim10() {
    // Create the tournament.
    $this->createTourney($this->controller, 10);

    $edit = array();
    // Match 1 Slot 1
    $edit['contestant1'] = 'user.10';
    // Match 1 Slot 2.
    $edit['contestant2'] = 'user.11';
    // Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

    // Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.11</div>');

    // Match 2
    $edit = array();
    $edit['contestant1'] = 'user.9';
	$edit['contestant2'] = 'user.12';
    $this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

    // Play Second Match
    $edit = array();
    $this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.12</div>');
    // Match 3
    $edit = array();
    $edit['contestant1'] = 'user.3';
    $this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

    // Play Third Match
    $edit = array();
    $this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    // Match 4
    $edit = array();
    $edit['contestant1'] = 'user.6';
    $edit['contestant2'] = 'user.7';
    $this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

    // Play Fourth Match
    $edit = array();
    $this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.7</div>');

    // Match 5
    $edit = array();
    $edit['contestant1'] = 'user.4';
    $this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

    // Play Fifth Match
    $edit = array();
    $this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

  	// Match 6
    $edit = array();
    $edit['contestant1'] = 'user.5';
    $edit['contestant2'] = 'user.8';
    $this->drupalPost('tourney/match/6/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

    // Play Sixth Match
    $edit = array();
    $this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');

    // Match 7 
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/7');
    $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.7</td>'));

    // Play Seventh Match
    $edit = array();
    $this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

    // Match 8
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/8');
    $this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.8</td>'));

    // Play Eighth Match
    $edit = array();
    $this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');

    // Match 9
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/9');
    $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.8</td>'));

    // Play Ninth Match
    $edit = array();
    $this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

    // Checking 10 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    // End of 10 Man Tourney
    $this->pass("End of 10 Man Single Elimination Tournament ");
   }

  /**
   * Testing 11 contestants
   */
  public function testSingleElim11() {
    // Create the tournament.
    $this->createTourney($this->controller, 11);

    $edit = array();
    // Match 1 Slot 1
    $edit['contestant1'] = 'user.10';
    // Match 1 Slot 2.
    $edit['contestant2'] = 'user.11';
    // Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

    // Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.11</div>');

    // Match 2
    $edit = array();
    $edit['contestant1'] = 'user.9';
	$edit['contestant2'] = 'user.12';
    $this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

    // Play Second Match
    $edit = array();
    $this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.12</div>');

    // Match 3
    $edit = array();
    $edit['contestant1'] = 'user.8';
	$edit['contestant2'] = 'user.13';
    $this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

    // Play Third Match
    $edit = array();
    $this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

    // Match 4
    $edit = array();
    $edit['contestant1'] = 'user.3';
    $this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

    // Play Fourth Match
    $edit = array();
    $this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

    // Match 5
    $edit = array();
    $edit['contestant1'] = 'user.6';
	$edit['contestant2'] = 'user.7';
    $this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

    // Play Fifth Match
    $edit = array();
    $this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.7</div>');

  	// Match 6
    $edit = array();
    // Match 5 Slot 1
    $edit['contestant1'] = 'user.4';
    $this->drupalPost('tourney/match/6/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

    // Play Sixth Match
    $edit = array();
    $this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

  	// Match 7
    $edit = array();
    $edit['contestant1'] = 'user.5';
    $this->drupalPost('tourney/match/7/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

    // Play Seventh Match
    $edit = array();
    $this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

    // Match 8 
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/8');
    $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.7</td>'));

    // Play Eighth Match
    $edit = array();
    $this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    // Match 9
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/9');
    $this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.13</td>'));

    // Play Ninth Match
    $edit = array();
    $this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

    // Match 10
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/10');
    $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.13</td>'));

    // Play Tenth Match
    $edit = array();
    $this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

    // Checking 11 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    // End of 11 Man Tourney
    $this->pass("End of 11 Man Single Elimination Tournament ");
   }

  /**
   * Testing 12 contestants
   */
  public function testSingleElim12() {
    // Create the tournament.
    $this->createTourney($this->controller, 12);

    $edit = array();
    // Match 1 Slot 1
    $edit['contestant1'] = 'user.10';
    // Match 1 Slot 2.
    $edit['contestant2'] = 'user.11';
    // Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

    // Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.11</div>');

    // Match 2
    $edit = array();
    $edit['contestant1'] = 'user.7';
	$edit['contestant2'] = 'user.14';
    $this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

    // Play Second Match
    $edit = array();
    $this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.14</div>');

    // Match 3
    $edit = array();
    $edit['contestant1'] = 'user.9';
	$edit['contestant2'] = 'user.12';
    $this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

    // Play Third Match
    $edit = array();
    $this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.12</div>');

    // Match 4
    $edit = array();
    $edit['contestant1'] = 'user.8';
	$edit['contestant2'] = 'user.13';
    $this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

    // Play Fourth Match
    $edit = array();
    $this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

    // Match 5
    $edit = array();
    $edit['contestant1'] = 'user.3';
    $this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

    // Play Fifth Match
    $edit = array();
    $this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

  	// Match 6
    $edit = array();
    $edit['contestant1'] = 'user.6';
    $this->drupalPost('tourney/match/6/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

    // Play Sixth Match
    $edit = array();
    $this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.14</div>');
  	// Match 7
    $edit = array();
    $edit['contestant1'] = 'user.4';
    $this->drupalPost('tourney/match/7/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

    // Play Seventh Match
    $edit = array();
    $this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');
	 // Match 8
    $edit = array();
    $edit['contestant1'] = 'user.5';
    $this->drupalPost('tourney/match/8/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

    // Play Eighth Match
    $edit = array();
    $this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

    // Match 9 
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/9');
    $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.14</td>'));

    // Play Ninth Match
    $edit = array();
    $this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 10
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/10');
    $this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.13</td>'));

    // Play Tenth Match
    $edit = array();
    $this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

    // Match 11
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/11');
    $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.13</td>'));

    // Play Eleventh Match
    $edit = array();
    $this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

    // Checking 12 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    // End of 12 Man Tourney
    $this->pass("End of 12 Man Single Elimination Tournament ");
   }


	/**
	 * Testing 13 contestants
	 */
	public function testSingleElim13() {
	 // Create the tournament.
	 $this->createTourney($this->controller, 13);

	 $edit = array();
	 // Match 1 Slot 1
	 $edit['contestant1'] = 'user.10';
	 // Match 1 Slot 2.
	 $edit['contestant2'] = 'user.11';
	 // Save Match 1 (/tourney/match/1)
	 $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
	 // Confirm Match has been saved.
	 $this->assertRaw(t('Your match has been saved'));

	 // Play First Match
	 $edit = array();
	 $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	 // Check to see if winner was successfully set passing in "Players moved to next round" text.
	 $this->assertRaw(t('Players moved to next round'));
	 // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.11</div>');

	 // Match 2
	 $edit = array();
	 $edit['contestant1'] = 'user.6';
	 $edit['contestant2'] = 'user.15';
	 $this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
	 // Confirm Match has been saved.
	 $this->assertRaw(t('Your match has been saved'));

	 // Play Second Match
	 $edit = array();
	 $this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	 // Check to see if winner was successfully set, passing in "Players moved to next round" text.
	 $this->assertRaw(t('Players moved to next round'));
	 // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

	 // Match 3
	 $edit = array();
	 $edit['contestant1'] = 'user.7';
	 $edit['contestant2'] = 'user.14';
	 $this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
	 // Confirm Match has been saved.
	 $this->assertRaw(t('Your match has been saved'));

	 // Play Third Match
	 $edit = array();
	 $this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	 // Check to see if winner was successfully set, passing in "Players moved to next round" text.
	 $this->assertRaw(t('Players moved to next round'));
	 // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.14</div>');

	 // Match 4
	 $edit = array();
	 $edit['contestant1'] = 'user.9';
	 $edit['contestant2'] = 'user.12';
	 $this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
	 // Confirm Match has been saved.
	 $this->assertRaw(t('Your match has been saved'));

	 // Play Fourth Match
	 $edit = array();
	 $this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	 // Check to see if winner was successfully set, passing in "Players moved to next round" text.
	 $this->assertRaw(t('Players moved to next round'));
	 // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.12</div>');

	 // Match 5
	 $edit = array();
	 $edit['contestant1'] = 'user.8';
	 $edit['contestant2'] = 'user.13';
	 $this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
	 // Confirm Match has been saved.
	 $this->assertRaw(t('Your match has been saved'));

	 // Play Fifth Match
	 $edit = array();
	 $this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	 // Check to see if winner was successfully set, passing in "Players moved to next round" text.
	 $this->assertRaw(t('Players moved to next round'));
	 // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

	// Match 6
	 $edit = array();
	 $edit['contestant1'] = 'user.3';
	 $this->drupalPost('tourney/match/6/edit', $edit, t('Save'));
	 // Confirm Match has been saved.
	 $this->assertRaw(t('Your match has been saved'));

	 // Play Sixth Match
	 $edit = array();
	 $this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	 // Check to see if winner was successfully set, passing in "Players moved to next round" text.
	 $this->assertRaw(t('Players moved to next round'));
	 // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	 // Match 7 
	 // Check to see if winner of previous match is being passed into this match correctly 
	 $this->drupalGet('tourney/match/7');
	 $this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
	 $this->assertRaw(t('<tr class="even"><td>user.14</td>'));

	 // Play Seventh Match
	 $edit = array();
	 $this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	 // Check to see if winner was successfully set, passing in "Players moved to next round" text.
	 $this->assertRaw(t('Players moved to next round'));
	 // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.14</div>');

	// Match 8
	 $edit = array();
	 $edit['contestant1'] = 'user.4';
	 $this->drupalPost('tourney/match/8/edit', $edit, t('Save'));
	 // Confirm Match has been saved.
	 $this->assertRaw(t('Your match has been saved'));

	 // Play Eighth Match
	 $edit = array();
	 $this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	 // Check to see if winner was successfully set, passing in "Players moved to next round" text.
	 $this->assertRaw(t('Players moved to next round'));
	 // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 9
	 $edit = array();
	 // Match 9 Slot 1
	 $edit['contestant1'] = 'user.5';
	 $this->drupalPost('tourney/match/9/edit', $edit, t('Save'));
	 // Confirm Match has been saved.
	 $this->assertRaw(t('Your match has been saved'));

	 // Play Ninth Match
	 $edit = array();
	 $this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	 // Check to see if winner was successfully set, passing in "Players moved to next round" text.
	 $this->assertRaw(t('Players moved to next round'));
	 // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

	 // Match 10 
	 // Check to see if winner of previous match is being passed into this match correctly 
	 $this->drupalGet('tourney/match/10');
	 $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
	 $this->assertRaw(t('<tr class="even"><td>user.14</td>'));

	 // Play Tenth Match
	 $edit = array();
	 $this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	 // Check to see if winner was successfully set, passing in "Players moved to next round" text.
	 $this->assertRaw(t('Players moved to next round'));
	 // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	 // Match 11
	 // Check to see if winner of previous match is being passed into this match correctly 
	 $this->drupalGet('tourney/match/11');
	 $this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
	 $this->assertRaw(t('<tr class="even"><td>user.13</td>'));

	 // Play Eleventh Match
	 $edit = array();
	 $this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	 // Check to see if winner was successfully set, passing in "Players moved to next round" text.
	 $this->assertRaw(t('Players moved to next round'));
	 // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

	 // Match 12
	 // Check to see if winner of previous match is being passed into this match correctly 
	 $this->drupalGet('tourney/match/12');
	 $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
	 $this->assertRaw(t('<tr class="even"><td>user.13</td>'));

	 // Play Twelfth Match
	 $edit = array();
	 $this->drupalPost('tourney/match/12', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	 // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	 // Checking 13 man Tourney Winner is displaying on the Tournament page
	 $edit = array();
	 $this->drupalGet('tourney/tournament/1');
	 // Check to see if the winner is printed on the page.
	 $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
	 // End of 13 Man Tourney
	 $this->pass("End of 13 Man Single Elimination Tournament ");
	}

   /**
    * Testing 14 contestants
    */
	public function testSingleElim14() {
    // Create the tournament.
   	$this->createTourney($this->controller, 14);

    $edit = array();
	// Match 1 Slot 1 
    $edit['contestant1'] = 'user.10';
	// Match 1 Slot 2
    $edit['contestant2'] = 'user.11';
	// Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
	// Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

	// Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.11</div>');

	// Match 2
	$edit = array();
	$edit['contestant1'] = 'user.6';
	$edit['contestant2'] = 'user.15';
	$this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Second Match
	$edit = array();
	$this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

	// Match 3
  	$edit = array();
	$edit['contestant1'] = 'user.7';
	$edit['contestant2'] = 'user.14';
  	$this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
	// Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Third Match
	$edit = array();
	$this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.14</div>');

	// Match 4
  	$edit = array();
	$edit['contestant1'] = 'user.9';
	$edit['contestant2'] = 'user.12';
  	$this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourth Match
	$edit = array();
	$this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.12</div>');

	// Match 5
	$edit = array();
	$edit['contestant1'] = 'user.5';
	$edit['contestant2'] = 'user.16';
	$this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifth Match
	$edit = array();
	$this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

	// Match 6
  	$edit = array();
	$edit['contestant1'] = 'user.8';
	$edit['contestant2'] = 'user.13';
  	$this->drupalPost('tourney/match/6/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Sixth Match
	$edit = array();
	$this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

	// Match 7
	$edit = array();
	$edit['contestant1'] = 'user.3';
	$this->drupalPost('tourney/match/7/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Seventh Match
	$edit = array();
	$this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 8
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/8');
  	$this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.14</td>'));

	// Play Eighth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.14</div>');

	// Match 9
	$edit = array();
	$edit['contestant1'] = 'user.4';
	$this->drupalPost('tourney/match/9/edit', $edit, t('Save'));
	// Check to see if match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Ninth Match
	$edit = array();
	$this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 10
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/10');
  	$this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.13</td>'));

	// Play Tenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

	// Match 11
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/11');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.14</td>'));

	// Play Eleventh Match
  	$edit = array();
  	$this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 12
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/12');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.13</td>'));

	// Play Twelfth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/12', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');
	// Match 13
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/13');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.13</td>'));

	// Play Thirteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/13', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

    // Checking 14 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
	// End of 14 Man Tourney
    $this->pass("End of 14 Man Single Elimination Tournament ");
   }

  /**
   * Testing 15 contestants
   */
	public function testSingleElim15() {
    // Create the tournament.
   	$this->createTourney($this->controller, 15);
	
    $edit = array();
	// Match 1 Slot 1 
    $edit['contestant1'] = 'user.10';
	// Match 1 Slot 2
    $edit['contestant2'] = 'user.11';
	// Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
	// Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

	// Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.11</div>');
	// Match 2
	$edit = array();
	$edit['contestant1'] = 'user.6';
	$edit['contestant2'] = 'user.15';
	$this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Second Match
	$edit = array();
	$this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

	// Match 3
  	$edit = array();
	$edit['contestant1'] = 'user.7';
	$edit['contestant2'] = 'user.14';
  	$this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Third Match
	$edit = array();
	$this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.14</div>');

	// Match 4
	$edit = array();
	$edit['contestant1'] = 'user.4';
	$edit['contestant2'] = 'user.17';
	$this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourth Match
	$edit = array();
	$this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 5
  	$edit = array();
	$edit['contestant1'] = 'user.9';
	$edit['contestant2'] = 'user.12';
  	$this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifth Match
	$edit = array();
	$this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.12</div>');

	// Match 6
	$edit = array();
	$edit['contestant1'] = 'user.5';
	$edit['contestant2'] = 'user.16';
	$this->drupalPost('tourney/match/6/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Sixth Match
	$edit = array();
	$this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

	// Match 7
  	$edit = array();
	$edit['contestant1'] = 'user.8';
	$edit['contestant2'] = 'user.13';
  	$this->drupalPost('tourney/match/7/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Seventh Match
	$edit = array();
	$this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

	// Match 8
	$edit = array();
	$edit['contestant1'] = 'user.3';
	$this->drupalPost('tourney/match/8/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eighth Match
	$edit = array();
	$this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
	
	// Match 9
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/9');
  	$this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.14</td>'));

	// Play Ninth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.14</div>');
	
	// Match 10
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/10');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.12</td>'));

	// Play Tenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');
	
	// Match 11
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/11');
  	$this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.13</td>'));

	// Play Eleventh Match
  	$edit = array();
  	$this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');
	
	// Match 12
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/12');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.14</td>'));

	// Play Twelfth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/12', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
	
	// Match 13
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/13');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.13</td>'));

	// Play Thirteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/13', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');
	
	// Match 14
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/14');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.13</td>'));

	// Play Fourteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/14', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

    // Checking 15 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
	// End of 15 Man Tourney
    $this->pass("End of 15 Man Single Elimination Tournament ");
   }

  /**
   * Testing 16 contestants
   */
  public function testSingleElim16() {
    // Create the tournament.
    $this->createTourney($this->controller, 16);
    
    $edit = array();
    // Match 1 Slot 1
    $edit['contestant1'] = 'user.3';
    // Match 1 Slot 2.
    $edit['contestant2'] = 'user.18';
    // Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    
    // Set contestants of the Second Match.
    $edit = array();
    $edit['contestant1'] = 'user.10';
    $edit['contestant2'] = 'user.11';
    $this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Second Match
    $edit = array();
    $this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.11</div>');
    
    // Set contestants of the Third Match.
    $edit = array();
    // Match 3 Slot 1
    $edit['contestant1'] = 'user.6';
    $edit['contestant2'] = 'user.15';
    $this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Third Match
    $edit = array();
    $this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

    // Set contestants of the Fourth Match.
    $edit = array();
    $edit['contestant1'] = 'user.7';
    $edit['contestant2'] = 'user.14';
    $this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Fourth Match
    $edit = array();
    $this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.14</div>');
    
    // Set contestants of the Fifth Match.
    $edit = array();
    $edit['contestant1'] = 'user.4';
    $edit['contestant2'] = 'user.17';
    $this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Fifth Match
    $edit = array();
    $this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');
    
    // Set contestants of the Sixth Match.
    $edit = array();
    $edit['contestant1'] = 'user.9';
    $edit['contestant2'] = 'user.12';
    $this->drupalPost('tourney/match/6/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Sixth Match
    $edit = array();
    $this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.12</div>');
    
    // Set contestants of the Seventh Match.
    $edit = array();
    $edit['contestant1'] = 'user.5';
    $edit['contestant2'] = 'user.16';
    $this->drupalPost('tourney/match/7/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Seventh Match
    $edit = array();
    $this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');
    
    // Set contestants of the Eighth Match.
    $edit = array();
    $edit['contestant1'] = 'user.8';
    $edit['contestant2'] = 'user.13';
    $this->drupalPost('tourney/match/8/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Eighth Match
    $edit = array();
    $this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');
    
    // Match 9
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/9');
    $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.11</td>'));
    
    // Play Ninth Match
    $edit = array();
    $this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    
    // Match 10
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/10');
    $this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.14</td>'));
    
    // Play Tenth Match
    $edit = array();
    $this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.14</div>');
    
    // Match 11
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/11');
    $this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.12</td>'));
    
    // Play Eleventh Match
    $edit = array();
    $this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');
    
    // Match 12
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/12');
    $this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.13</td>'));
    
    // Play Twelfth Match
    $edit = array();
    $this->drupalPost('tourney/match/12', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');
    
    // Match 13
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/13');
    $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.14</td>'));
    
    // Play Thirteenth Match
    $edit = array();
    $this->drupalPost('tourney/match/13', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    
    // Match 14
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/14');
    $this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.13</td>'));
    
    // Play Fourteenth Match
    $edit = array();
    $this->drupalPost('tourney/match/14', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');
    
    // Match 15
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/15');
    $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.13</td>'));
    
    // Play Fifthteenth Match
    $edit = array();
    $this->drupalPost('tourney/match/15', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if the winner is printed on the page.
    $this->assertRaw(t('user.3'));
    
    // Checking 16 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    // End of 16 Man Tourney
    $this->pass("End of 16 Man Single Elimination Tournament ");
   }
  
  /**
   * Testing 17 contestants
   */
	public function testSingleElim17() {
    // Create the tournament.
   	$this->createTourney($this->controller, 17);

	// Upper Bracket
    $edit = array();
	// Match 1 Slot 1 
    $edit['contestant1'] = 'user.18';
	// Match 1 Slot 2
    $edit['contestant2'] = 'user.19';
	// Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
	// Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

	// Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.19</div>');

	// Match 2
	$edit = array();
	$edit['contestant1'] = 'user.3';
	$this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Second Match
	$edit = array();
	$this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 3
  	$edit = array();
	$edit['contestant1'] = 'user.10';
	$edit['contestant2'] = 'user.11';
  	$this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Third Match
	$edit = array();
	$this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.11</div>');

	// Match 4
	$edit = array();
	$edit['contestant1'] = 'user.6';
	$edit['contestant2'] = 'user.15';
	$this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourth Match
	$edit = array();
	$this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

	// Match 5
  	$edit = array();
	$edit['contestant1'] = 'user.7';
	$edit['contestant2'] = 'user.14';
  	$this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifth Match
	$edit = array();
	$this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.14</div>');

	// Match 6
	$edit = array();
	$edit['contestant1'] = 'user.4';
	$edit['contestant2'] = 'user.17';
	$this->drupalPost('tourney/match/6/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Sixth Match
	$edit = array();
	$this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');		

	// Match 7
  	$edit = array();
	$edit['contestant1'] = 'user.9';
	$edit['contestant2'] = 'user.12';
  	$this->drupalPost('tourney/match/7/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Seventh Match
	$edit = array();
	$this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.12</div>');	

	// Match 8
	$edit = array();
	$edit['contestant1'] = 'user.5';
	$edit['contestant2'] = 'user.16';
	$this->drupalPost('tourney/match/8/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eighth Match
	$edit = array();
	$this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

	// Match 9
  	$edit = array();
	$edit['contestant1'] = 'user.8';
	$edit['contestant2'] = 'user.13';
  	$this->drupalPost('tourney/match/9/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Ninth Match
	$edit = array();
	$this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

	// Match 10
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/10');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.11</td>'));

	// Play Tenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 11
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/11');
  	$this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.14</td>'));

	// Play Eleventh Match
  	$edit = array();
  	$this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.14</div>');

	// Match 12
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/12');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.12</td>'));

	// Play Twelfth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/12', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 13
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/13');
  	$this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.13</td>'));

	// Play Thirteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/13', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

	// Match 14
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/14');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.14</td>'));

	// Play Fourteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/14', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 15
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/15');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.13</td>'));

	// Play Fifteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/15', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

	// Match 16
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/16');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.13</td>'));

	// Play Sixteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/16', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

    // Checking 17 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
	// End of 17 Man Tourney
    $this->pass("End of 17 Man Single Elimination Tournament ");
   }


  /**
   * Testing 18 contestants
   */
	public function testSingleElim18() {
    // Create the tournament.
   	$this->createTourney($this->controller, 18);
	
	// Upper Bracket
    $edit = array();
	// Match 1 Slot 1
    $edit['contestant1'] = 'user.18';
	// Match 1 Slot 2
    $edit['contestant2'] = 'user.19';
	// Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
	// Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

	// Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.19</div>');

	// Match 2
  	$edit = array();
	$edit['contestant1'] = 'user.17';
	$edit['contestant2'] = 'user.20';
  	$this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Plat Second Match
	$edit = array();
	$this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.20</div>');

	// Match 3
	$edit = array();
	$edit['contestant1'] = 'user.3';
	$this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Third Match
	$edit = array();
	$this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 4
  	$edit = array();
	$edit['contestant1'] = 'user.10';
	$edit['contestant2'] = 'user.11';
  	$this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourth Match
	$edit = array();
	$this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.11</div>');

	// Match 5
	$edit = array();
	$edit['contestant1'] = 'user.6';
	$edit['contestant2'] = 'user.15';
	$this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifth Match
	$edit = array();
	$this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

	// Match 6
  	$edit = array();
	$edit['contestant1'] = 'user.7';
	$edit['contestant2'] = 'user.14';
  	$this->drupalPost('tourney/match/6/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Sixth Match
	$edit = array();
	$this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.14</div>');

	// Match 7
	$edit = array();
	$edit['contestant1'] = 'user.4';
	$this->drupalPost('tourney/match/7/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Seventh Match
	$edit = array();
	$this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 8
  	$edit = array();
	$edit['contestant1'] = 'user.9';
	$edit['contestant2'] = 'user.12';
  	$this->drupalPost('tourney/match/8/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eighth Match
	$edit = array();
	$this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.12</div>');

	// Match 9
	$edit = array();
	$edit['contestant1'] = 'user.5';
	$edit['contestant2'] = 'user.16';
	$this->drupalPost('tourney/match/9/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Ninth Match
	$edit = array();
	$this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

	// Match 10
  	$edit = array();
	$edit['contestant1'] = 'user.8';
	$edit['contestant2'] = 'user.13';
  	$this->drupalPost('tourney/match/10/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Tenth Match
	$edit = array();
	$this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

	// Match 11
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/11');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.11</td>'));

	// Play Eleventh Match
  	$edit = array();
  	$this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 12
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/12');
  	$this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.14</td>'));

	// Play Twelfth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/12', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.14</div>');

	// Match 13
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/13');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.12</td>'));

	// Play Thirteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/13', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 14
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/14');
  	$this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.13</td>'));

	// Play Fourteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/14', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

	// Match 15
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/15');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.14</td>'));

	// Play Fifteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/15', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 16
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/16');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.13</td>'));

	// Play Sixteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/16', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

	// Match 17
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/17');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.13</td>'));

	// Play Seventeenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/17', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Checking 25 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
	// End of 18 Man Tourney
    $this->pass("End of 18 Man Single Elimination Tournament ");
   }

   /**
   * Testing 19 contestants
   */
	public function testSingleElim19() {
    // Create the tournament.
   	$this->createTourney($this->controller, 19);
	
	// Upper Bracket
    $edit = array();
	// Match 1 Slot 1
    $edit['contestant1'] = 'user.18';
	// Match 1 Slot 2
    $edit['contestant2'] = 'user.19';
	// Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
	// Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

	// Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.19</div>');

	// Match 2
  	$edit = array();
	$edit['contestant1'] = 'user.17';
	$edit['contestant2'] = 'user.20';
  	$this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Second Match
	$edit = array();
	$this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.20</div>');

	// Match 3
  	$edit = array();
	$edit['contestant1'] = 'user.16';
	$edit['contestant2'] = 'user.21';
  	$this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Third Match
	$edit = array();
	$this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.21</div>');

	// Match 4
	$edit = array();
	$edit['contestant1'] = 'user.3';
	$this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourth Match
	$edit = array();
	$this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 5
  	$edit = array();
	$edit['contestant1'] = 'user.10';
	$edit['contestant2'] = 'user.11';
  	$this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifth Match
	$edit = array();
	$this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.11</div>');

	// Match 6
	$edit = array();
	$edit['contestant1'] = 'user.6';
	$edit['contestant2'] = 'user.15';
	$this->drupalPost('tourney/match/6/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Sixth Match
	$edit = array();
	$this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

	// Match 7
  	$edit = array();
	$edit['contestant1'] = 'user.7';
	$edit['contestant2'] = 'user.14';
  	$this->drupalPost('tourney/match/7/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Seventh Match 
	$edit = array();
	$this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.14</div>');

	// Match 8
	$edit = array();
	$edit['contestant1'] = 'user.4';
	$this->drupalPost('tourney/match/8/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eighth Match
	$edit = array();
	$this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 9
  	$edit = array();
	$edit['contestant1'] = 'user.9';
	$edit['contestant2'] = 'user.12';
  	$this->drupalPost('tourney/match/9/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Ninth Match
	$edit = array();
	$this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.12</div>');

	// Match 10
	$edit = array();
	$edit['contestant1'] = 'user.5';
	$this->drupalPost('tourney/match/10/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Tenth Match
	$edit = array();
	$this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

	// Match 11
  	$edit = array();
	$edit['contestant1'] = 'user.8';
	$edit['contestant2'] = 'user.13';
  	$this->drupalPost('tourney/match/11/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eleventh Match
	$edit = array();
	$this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

	// Match 12
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/12');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.11</td>'));

	// Play Twelfth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/12', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 13
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/13');
  	$this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.14</td>'));

	// Play Thirteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/13', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.14</div>');

	// Match 14
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/14');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.12</td>'));

	// Play Fourteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/14', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 15
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/15');
  	$this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.13</td>'));

	// Play Fifteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/15', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

	// Match 16
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/16');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.14</td>'));

	// Play Sixteenthing Match
  	$edit = array();
  	$this->drupalPost('tourney/match/16', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 17
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/17');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.13</td>'));

	// Play Seventeenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/17', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

	// Match 18
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/18');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.13</td>'));

	// Play Eighteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/18', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Checking 19 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
	// End of 19 Man Tourney
    $this->pass("End of 19 Man Single Elimination Tournament ");
   }

  /**
   * Testing 20 contestants
   */
	public function testSingleElim20() {
    // Create the tournament.
   	$this->createTourney($this->controller, 20);
	
	// Upper Bracket
    $edit = array();
	// Match 1 Slot 1 
    $edit['contestant1'] = 'user.18';
	// Match 1 Slot 2
    $edit['contestant2'] = 'user.19';
	// Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
	// Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

	// Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.19</div>');

	// Match 2
  	$edit = array();
	$edit['contestant1'] = 'user.15';
	$edit['contestant2'] = 'user.22';
  	$this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Second Match
	$edit = array();
	$this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.22</div>');

	// Match 3
  	$edit = array();
	$edit['contestant1'] = 'user.17';
	$edit['contestant2'] = 'user.20';
  	$this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Third Match
	$edit = array();
	$this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.20</div>');

	// Match 4
  	$edit = array();
	$edit['contestant1'] = 'user.16';
	$edit['contestant2'] = 'user.21';
  	$this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourth Match
	$edit = array();
	$this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.21</div>');

	// Match 5
	$edit = array();
	$edit['contestant1'] = 'user.3';
	$this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifth Match
	$edit = array();
	$this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 6
  	$edit = array();
	$edit['contestant1'] = 'user.10';
	$edit['contestant2'] = 'user.11';
  	$this->drupalPost('tourney/match/6/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Sixth Match
	$edit = array();
	$this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.11</div>');

	// Match 7
	$edit = array();
	$edit['contestant1'] = 'user.6';
	$this->drupalPost('tourney/match/7/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Seventh Match
	$edit = array();
	$this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

	// Match 8
  	$edit = array();
	$edit['contestant1'] = 'user.7';
	$edit['contestant2'] = 'user.14';
  	$this->drupalPost('tourney/match/8/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eighth Match
	$edit = array();
	$this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.14</div>');

	// Match 9
	$edit = array();
	$edit['contestant1'] = 'user.4';
	$this->drupalPost('tourney/match/9/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Ninth Match
	$edit = array();
	$this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 10
  	$edit = array();
	$edit['contestant1'] = 'user.9';
	$edit['contestant2'] = 'user.12';
  	$this->drupalPost('tourney/match/10/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Tenth Match
	$edit = array();
	$this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.12</div>');

	// Match 11
	$edit = array();
	$edit['contestant1'] = 'user.5';
	$this->drupalPost('tourney/match/11/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eleventh Match
	$edit = array();
	$this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

	// Match 12
  	$edit = array();
	$edit['contestant1'] = 'user.8';
	$edit['contestant2'] = 'user.13';
  	$this->drupalPost('tourney/match/12/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Twelfth Match
	$edit = array();
	$this->drupalPost('tourney/match/12', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

	// Match 13
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/13');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.11</td>'));

	// Play Thirteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/13', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 14
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/14');
  	$this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.14</td>'));

	// Play Fourteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/14', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.14</div>');

	// Match 15
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/15');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.12</td>'));

	// Play Fifteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/15', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 16
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/16');
  	$this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.13</td>'));

	// Play Sixteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/16', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

	// Match 17
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/17');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.14</td>'));

	// Play Seventeenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/17', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 18
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/18');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.13</td>'));

	// Play Eighteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/18', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

	// Match 19 
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/19');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.13</td>'));

	// Play Ninteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/19', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Checking 20 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
	// End of 20 Man Tourney
    $this->pass("End of 20 Man Single Elimination Tournament ");
   }

  /**
   * Testing 21 contestants
   */
	public function testSingleElim21() {
    // Create the tournament.
   	$this->createTourney($this->controller, 21);
	
	// Set Upper Bracket
    $edit = array();
	// Match 1 Slot 1 
    $edit['contestant1'] = 'user.18';
	// Match 1 Slot 2
    $edit['contestant2'] = 'user.19';
	// Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
	// Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

	// Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.19</div>');

	// Match 2
  	$edit = array();
	$edit['contestant1'] = 'user.15';
	$edit['contestant2'] = 'user.22';
  	$this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Second Match
	$edit = array();
	$this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.22</div>');

	// Match 3
  	$edit = array();
	$edit['contestant1'] = 'user.14';
	$edit['contestant2'] = 'user.23';
  	$this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Third Match
	$edit = array();
	$this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 4
  	$edit = array();
	$edit['contestant1'] = 'user.17';
	$edit['contestant2'] = 'user.20';
  	$this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourth Match
	$edit = array();
	$this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.20</div>');

	// Match 5
  	$edit = array();
	$edit['contestant1'] = 'user.16';
	$edit['contestant2'] = 'user.21';
  	$this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifth Match
	$edit = array();
	$this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.21</div>');

	// Match 6
	$edit = array();
	$edit['contestant1'] = 'user.3';
	$this->drupalPost('tourney/match/6/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Sixth Match
	$edit = array();
	$this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 7
  	$edit = array();
	$edit['contestant1'] = 'user.10';
	$edit['contestant2'] = 'user.11';
  	$this->drupalPost('tourney/match/7/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Seventh Match
	$edit = array();
	$this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.11</div>');

	// Match 8
	$edit = array();
	$edit['contestant1'] = 'user.6';
	$this->drupalPost('tourney/match/8/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eighth Match
	$edit = array();
	$this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

	// Match 9
  	$edit = array();
	$edit['contestant1'] = 'user.7';
  	$this->drupalPost('tourney/match/9/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Ninth Match
	$edit = array();
	$this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 10
	$edit = array();
	$edit['contestant1'] = 'user.4';
	$this->drupalPost('tourney/match/10/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Tenth Match
	$edit = array();
	$this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 11
  	$edit = array();
	$edit['contestant1'] = 'user.9';
	$edit['contestant2'] = 'user.12';
  	$this->drupalPost('tourney/match/11/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eleventh Match
	$edit = array();
	$this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.12</div>');

	// Match 12
	$edit = array();
	$edit['contestant1'] = 'user.5';
	$this->drupalPost('tourney/match/12/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Twelfth Match
	$edit = array();
	$this->drupalPost('tourney/match/12', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
	$this->assertRaw(t('user.5'));

	// Match 13
  	$edit = array();
	$edit['contestant1'] = 'user.8';
	$edit['contestant2'] = 'user.13';
  	$this->drupalPost('tourney/match/13/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Thirteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/13', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

	// Match 14
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/14');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.11</td>'));

	// Play Fourteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/14', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 15
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/15');
  	$this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Fifteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/15', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 16
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/16');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.12</td>'));

	// Play Sixteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/16', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 17
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/17');
  	$this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.13</td>'));

	// Play Seventeenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/17', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

	// Match 18
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/18');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Eighteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/18', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 19
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/19');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.13</td>'));

	// Play Ninteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/19', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.13</div>');

	// Match 20
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/20');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.13</td>'));

	// Play Twentieth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/20', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Checking 25 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
	// End of 21 Man Tourney
    $this->pass("End of 21 Man Single Elimination Tournament ");
   }

	/**
     * Testing 22 contestants
     */
	public function testSingleElim22() {
    // Create the tournament.
   	$this->createTourney($this->controller, 22);
	
	// Set Upper Bracket
    $edit = array();
	// Match 1 Slot 1 (odd)
    $edit['contestant1'] = 'user.18';
	// Match 1 Slot 2. (even)
    $edit['contestant2'] = 'user.19';
	// Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
	// Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

	// Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.19</div>');

	// Match 2
  	$edit = array();
	$edit['contestant1'] = 'user.15';
	$edit['contestant2'] = 'user.22';
  	$this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Second Match
	$edit = array();
	$this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.22</div>');

	// Match 3
  	$edit = array();
	$edit['contestant1'] = 'user.14';
	$edit['contestant2'] = 'user.23';
  	$this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Third Match
	$edit = array();
	$this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 4
  	$edit = array();
	$edit['contestant1'] = 'user.17';
	$edit['contestant2'] = 'user.20';
  	$this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourth Match
	$edit = array();
	$this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.20</div>');

	// Match 5
  	$edit = array();
	//Contestants
	$edit['contestant1'] = 'user.16';
	$edit['contestant2'] = 'user.21';
  	$this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifth Match
	$edit = array();
	$this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.21</div>');

	// Match 6
  	$edit = array();
	$edit['contestant1'] = 'user.13';
	$edit['contestant2'] = 'user.24';
  	$this->drupalPost('tourney/match/6/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Sixth Match
	$edit = array();
	$this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 7
	$edit = array();
	$edit['contestant1'] = 'user.3';
	$this->drupalPost('tourney/match/7/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Seventh Match
	$edit = array();
	$this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 8
  	$edit = array();
	$edit['contestant1'] = 'user.10';
	$edit['contestant2'] = 'user.11';
  	$this->drupalPost('tourney/match/8/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eighth Match
	$edit = array();
	$this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.11</div>');

	// Match 9
	$edit = array();
	$edit['contestant1'] = 'user.6';
	$this->drupalPost('tourney/match/9/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Ninth Match
	$edit = array();
	$this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

	// Match 10
  	$edit = array();
	$edit['contestant1'] = 'user.7';
  	$this->drupalPost('tourney/match/10/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Tenth Match
	$edit = array();
	$this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 11
	$edit = array();
	$edit['contestant1'] = 'user.4';
	$this->drupalPost('tourney/match/11/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eleventh Match
	$edit = array();
	$this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 12
  	$edit = array();
	$edit['contestant1'] = 'user.9';
	$edit['contestant2'] = 'user.12';
  	$this->drupalPost('tourney/match/12/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Twelfth Match
	$edit = array();
	$this->drupalPost('tourney/match/12', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.12</div>');

	// Match 13
	$edit = array();
	$edit['contestant1'] = 'user.5';
	$this->drupalPost('tourney/match/13/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Thirteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/13', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

	// Match 14
	$edit = array();
	$edit['contestant1'] = 'user.8';
	$this->drupalPost('tourney/match/14/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/14', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 15
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/15');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.11</td>'));

	// Play Fifteenth Match 
  	$edit = array();
  	$this->drupalPost('tourney/match/15', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 16
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/16');
  	$this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Sixteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/16', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 17
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/17');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.12</td>'));

	// Play Seventeenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/17', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 18
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/18');
  	$this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Eighteenting Match
  	$edit = array();
  	$this->drupalPost('tourney/match/18', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 19
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/19');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Ninteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/19', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 20
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/20');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twentieth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/20', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 21
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/21');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty First Match
  	$edit = array();
  	$this->drupalPost('tourney/match/21', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Checking 22 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
	// End of 22 Man Tourney
    $this->pass("End of 22 Man Single Elimination Tournament ");
   }


  /**
   * Testing 23 contestants
   */
	public function testSingleElim23() {
    // Create the tournament.
   	$this->createTourney($this->controller, 23);

	// Upper Bracket
    $edit = array();
	// Match 1 Slot 1
    $edit['contestant1'] = 'user.18';
	// Match 1 Slot 2
    $edit['contestant2'] = 'user.19';
	// Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
	// Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

	// Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw(t('user.19'));

	// Match 2
  	$edit = array();
	$edit['contestant1'] = 'user.15';
	$edit['contestant2'] = 'user.22';
  	$this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Second Match
	$edit = array();
	$this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.22</div>');

	// Match 3
  	$edit = array();
	$edit['contestant1'] = 'user.14';
	$edit['contestant2'] = 'user.23';
  	$this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Third Match
	$edit = array();
	$this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 4
  	$edit = array();
	$edit['contestant1'] = 'user.17';
	$edit['contestant2'] = 'user.20';
  	$this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourth Match
	$edit = array();
	$this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.20</div>');

	// Match 5
  	$edit = array();
	$edit['contestant1'] = 'user.12';
	$edit['contestant2'] = 'user.25';
  	$this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifth Match
	$edit = array();
	$this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.25</div>');

	// Match 6
  	$edit = array();
	$edit['contestant1'] = 'user.16';
	$edit['contestant2'] = 'user.21';
  	$this->drupalPost('tourney/match/6/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Sixth Match
	$edit = array();
	$this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.21</div>');

	// Match 7
  	$edit = array();
	$edit['contestant1'] = 'user.13';
	$edit['contestant2'] = 'user.24';
  	$this->drupalPost('tourney/match/7/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Seventh Match
	$edit = array();
	$this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 8
  	$edit = array();
	$edit['contestant1'] = 'user.3';
  	$this->drupalPost('tourney/match/8/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eighth Match
	$edit = array();
	$this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 9
  	$edit = array();
	$edit['contestant1'] = 'user.10';
	$edit['contestant2'] = 'user.11';
  	$this->drupalPost('tourney/match/9/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Ninth Match
	$edit = array();
	$this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.11</div>');

	// Match 10
	$edit = array();
	$edit['contestant1'] = 'user.6';
	$this->drupalPost('tourney/match/10/edit', $edit, t('Save'));
	// Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Tenth Match
	$edit = array();
	$this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

	// Match 11
  	$edit = array();
	$edit['contestant1'] = 'user.7';
  	$this->drupalPost('tourney/match/11/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eleventh Match
	$edit = array();
	$this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 12
	$edit = array();
	$edit['contestant1'] = 'user.4';
	$this->drupalPost('tourney/match/12/edit', $edit, t('Save'));
	// Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Twelfth Match
	$edit = array();
	$this->drupalPost('tourney/match/12', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 13
  	$edit = array();
	$edit['contestant1'] = 'user.9';
  	$this->drupalPost('tourney/match/13/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Thirteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/13', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.25</div>');

	// Match 14
	$edit = array();
	$edit['contestant1'] = 'user.5';
	$this->drupalPost('tourney/match/14/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/14', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

	// Match 15
  	$edit = array();
	$edit['contestant1'] = 'user.8';
  	$this->drupalPost('tourney/match/15/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/15', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 16
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/16');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.11</td>'));

	// Play Sixteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/16', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 17
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/17');
  	$this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Seventeenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/17', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 18
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/18');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.25</td>'));

	// Play Eighteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/18', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 19
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/19');
  	$this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Ninteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/19', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 20
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/20');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Twentieth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/20', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 21
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/21');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty First Match
  	$edit = array();
  	$this->drupalPost('tourney/match/21', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 22
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/22');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Second Match
  	$edit = array();
  	$this->drupalPost('tourney/match/22', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Checking 23 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
	// End of 23 Man Tourney
    $this->pass("End of 23 Man Single Elimination Tournament ");
   }

  /**
   * Testing 24 contestants
   */
	public function testSingleElim24() {
    // Create the tournament.
   	$this->createTourney($this->controller, 24);
	
	// Upper Bracket
    $edit = array();
	// Match 1 Slot 1 
    $edit['contestant1'] = 'user.18';
	// Match 1 Slot 2
    $edit['contestant2'] = 'user.19';
	// Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
	// Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

	// Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.19</div>');

	// Match 2
  	$edit = array();
	$edit['contestant1'] = 'user.11';
	$edit['contestant2'] = 'user.26';
  	$this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Second Match
	$edit = array();
	$this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.26</div>');

	// Match 3
  	$edit = array();
	$edit['contestant1'] = 'user.15';
	$edit['contestant2'] = 'user.22';
  	$this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Third Match
	$edit = array();
	$this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.22</div>');

	// Match 4
  	$edit = array();
	$edit['contestant1'] = 'user.14';
	$edit['contestant2'] = 'user.23';
  	$this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourth Match
	$edit = array();
	$this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 5
  	$edit = array();
	$edit['contestant1'] = 'user.17';
	$edit['contestant2'] = 'user.20';
  	$this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifth Match
	$edit = array();
	$this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.20</div>');

	// Match 6
  	$edit = array();
	$edit['contestant1'] = 'user.12';
	$edit['contestant2'] = 'user.25';
  	$this->drupalPost('tourney/match/6/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Sixth Match
	$edit = array();
	$this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.25</div>');

	// Match 7
  	$edit = array();
	$edit['contestant1'] = 'user.16';
	$edit['contestant2'] = 'user.21';
  	$this->drupalPost('tourney/match/7/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Seventh Match
	$edit = array();
	$this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.21</div>');

	// Match 8
  	$edit = array();
	$edit['contestant1'] = 'user.13';
	$edit['contestant2'] = 'user.24';
  	$this->drupalPost('tourney/match/8/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eighth Match
	$edit = array();
	$this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');
	
	// Match 9
	$edit = array();
	$edit['contestant1'] = 'user.3';
	$this->drupalPost('tourney/match/9/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Ninth Match
	$edit = array();
	$this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 10
  	$edit = array();
	$edit['contestant1'] = 'user.10';
  	$this->drupalPost('tourney/match/10/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Tenth Match
	$edit = array();
	$this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.26</div>');

	// Match 11 
	$edit = array();
	$edit['contestant1'] = 'user.6';
	$this->drupalPost('tourney/match/11/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eleventh Match
	$edit = array();
	$this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

	// Match 12
  	$edit = array();
	$edit['contestant1'] = 'user.7';	
  	$this->drupalPost('tourney/match/12/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Twelfth Match
	$edit = array();
	$this->drupalPost('tourney/match/12', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 13
	$edit = array();
	$edit['contestant1'] = 'user.4';
	$this->drupalPost('tourney/match/13/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Thirteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/13', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 14
  	$edit = array();
	$edit['contestant1'] = 'user.9';
  	$this->drupalPost('tourney/match/14/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/14', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.25</div>');

	// Match 15
	$edit = array();
	$edit['contestant1'] = 'user.5';
	$this->drupalPost('tourney/match/15/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/15', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

	// Match 16
  	$edit = array();
	$edit['contestant1'] = 'user.8';
  	$this->drupalPost('tourney/match/16/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Sixteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/16', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');
	
	// Match 17
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/17');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.26</td>'));

	// Play Seventeenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/17', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
	
	// Match 18
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/18');
  	$this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Eighteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/18', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');
	
	// Match 19
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/19');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.25</td>'));

	// Play Ninteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/19', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');
	
	// Match 20
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/20');
  	$this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twentieth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/20', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 21
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/21');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Twenty First Match
  	$edit = array();
  	$this->drupalPost('tourney/match/21', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 22
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/22');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Second Match
  	$edit = array();
  	$this->drupalPost('tourney/match/22', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');
	
	// Match 23
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/23');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Third Match
  	$edit = array();
  	$this->drupalPost('tourney/match/23', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Checking 25 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
	// End of 24 Man Tourney
    $this->pass("End of 24 Man Single Elimination Tournament ");
   }

  /**
   * Testing 25 contestants
   */
	public function testSingleElim25() {
    // Create the tournament.
   	$this->createTourney($this->controller, 25);

	// Upper Bracket
    $edit = array();
	// Match 1 Slot 1
    $edit['contestant1'] = 'user.18';
	// Match 1 Slot 2
    $edit['contestant2'] = 'user.19';
	// Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
	// Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

	// Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.19</div>');

	// Match 2
	$edit = array();
	$edit['contestant1'] = 'user.10';
	$edit['contestant2'] = 'user.27';
	$this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Second Match
	$edit = array();
	$this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.10</div>');
	
	// Match 3
  	$edit = array();
	$edit['contestant1'] = 'user.11';
	$edit['contestant2'] = 'user.26';
  	$this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Third Match 
	$edit = array();
	$this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.26</div>');

	// Match 4
  	$edit = array();
	$edit['contestant1'] = 'user.15';
	$edit['contestant2'] = 'user.22';
  	$this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourth Match
	$edit = array();
	$this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.22</div>');
	
	// Match 5
  	$edit = array();
	$edit['contestant1'] = 'user.14';
	$edit['contestant2'] = 'user.23';
  	$this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifth Match
	$edit = array();
	$this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 6
  	$edit = array();
	$edit['contestant1'] = 'user.17';
	$edit['contestant2'] = 'user.20';
  	$this->drupalPost('tourney/match/6/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Sixth Match
	$edit = array();
	$this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.20</div>');

	// Match 7
  	$edit = array();
	$edit['contestant1'] = 'user.12';
	$edit['contestant2'] = 'user.25';
  	$this->drupalPost('tourney/match/7/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Seventh Match
	$edit = array();
	$this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.25</div>');

	// Match 8
  	$edit = array();
	$edit['contestant1'] = 'user.16';
	$edit['contestant2'] = 'user.21';
  	$this->drupalPost('tourney/match/8/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eighth Match
	$edit = array();
	$this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.21</div>');

	// Match 9
  	$edit = array();
	$edit['contestant1'] = 'user.13';
	$edit['contestant2'] = 'user.24';
  	$this->drupalPost('tourney/match/9/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Ninth Match
	$edit = array();
	$this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 10
	$edit = array();
	$edit['contestant1'] = 'user.3';
	$this->drupalPost('tourney/match/10/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Tenth Match
	$edit = array();
	$this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 11
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/11');
  	$this->assertRaw(t('<tr class="odd"><td>user.10</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.26</td>'));

	// Play Eleventh Match
  	$edit = array();
  	$this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.26</div>');

	// Match 12
	$edit = array();
	$edit['contestant1'] = 'user.6';
	$this->drupalPost('tourney/match/12/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Twelfth Match
	$edit = array();
	$this->drupalPost('tourney/match/12', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

	// Match 13
  	$edit = array();
	$edit['contestant1'] = 'user.7';
  	$this->drupalPost('tourney/match/13/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Thirteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/13', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 14
	$edit = array();
	$edit['contestant1'] = 'user.4';
	$this->drupalPost('tourney/match/14/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/14', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 15
  	$edit = array();
	$edit['contestant1'] = 'user.9';
  	$this->drupalPost('tourney/match/15/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/15', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.25</div>');

	// Match 16
	$edit = array();
	$edit['contestant1'] = 'user.5';
	$this->drupalPost('tourney/match/16/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Sixteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/16', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

	// Match 17
  	$edit = array();
	$edit['contestant1'] = 'user.8';
  	$this->drupalPost('tourney/match/17/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Seventeenth Match
	$edit = array();
	$this->drupalPost('tourney/match/17', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 18
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/18');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.26</td>'));

	// Play Eighteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/18', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 19
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/19');
  	$this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Ninteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/19', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 20
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/20');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.25</td>'));

	// Play Twentieth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/20', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 21
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/21');
  	$this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty First Match
  	$edit = array();
  	$this->drupalPost('tourney/match/21', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 22
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/22');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Twenty Second Match
  	$edit = array();
  	$this->drupalPost('tourney/match/22', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 23
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/23');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Third Match
  	$edit = array();
  	$this->drupalPost('tourney/match/23', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 24
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/24');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Fourth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/24', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Checking 25 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
	// End of 25 Man Tourney
    $this->pass("End of 25 Man Single Elimination Tournament ");
   }

  /**
   * Testing 26 contestants
   */
	public function testSingleElim26() {
    // Create the tournament.
   	$this->createTourney($this->controller, 26);

	// Upper Bracket
    $edit = array();
	// Match 1 Slot 1 
    $edit['contestant1'] = 'user.18';
	// Match 1 Slot 2
    $edit['contestant2'] = 'user.19';
	// Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
	// Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

	// Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.19</div>');

	// Match 2
	$edit = array();
	$edit['contestant1'] = 'user.10';
	$edit['contestant2'] = 'user.27';
	$this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Second Match
	$edit = array();
	$this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.10</div>');

	// Match 3
  	$edit = array();
	$edit['contestant1'] = 'user.11';
	$edit['contestant2'] = 'user.26';
  	$this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Third Match
	$edit = array();
	$this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.26</div>');

	// Match 4
  	$edit = array();
	$edit['contestant1'] = 'user.15';
	$edit['contestant2'] = 'user.22';
  	$this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourth Match
	$edit = array();
	$this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.22</div>');

	// Match 5
  	$edit = array();
	$edit['contestant1'] = 'user.14';
	$edit['contestant2'] = 'user.23';
  	$this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifth Match
	$edit = array();
	$this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 6
  	$edit = array();
	$edit['contestant1'] = 'user.17';
	$edit['contestant2'] = 'user.20';
  	$this->drupalPost('tourney/match/6/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Sixth Match
	$edit = array();
	$this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.20</div>');

	// Match 7
	$edit = array();
	$edit['contestant1'] = 'user.9';
	$edit['contestant2'] = 'user.28';
	$this->drupalPost('tourney/match/7/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Seventh Match
	$edit = array();
	$this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.9</div>');

	// Match 8
  	$edit = array();
	$edit['contestant1'] = 'user.12';
	$edit['contestant2'] = 'user.25';
  	$this->drupalPost('tourney/match/8/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eighth Match
	$edit = array();
	$this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.25</div>');

	// Match 9
  	$edit = array();
	$edit['contestant1'] = 'user.16';
	$edit['contestant2'] = 'user.21';
  	$this->drupalPost('tourney/match/9/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Ninth Match
	$edit = array();
	$this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.21</div>');

	// Match 10
  	$edit = array();
	$edit['contestant1'] = 'user.13';
	$edit['contestant2'] = 'user.24';
  	$this->drupalPost('tourney/match/10/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Tenth Match
	$edit = array();
	$this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 11
	$edit = array();
	$edit['contestant1'] = 'user.3';
	$this->drupalPost('tourney/match/11/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eleventh Match
	$edit = array();
	$this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 12
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/12');
  	$this->assertRaw(t('<tr class="odd"><td>user.10</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.26</td>'));

	// Play Twelfth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/12', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.26</div>');

	// Match 13
	$edit = array();
	$edit['contestant1'] = 'user.6';
	$this->drupalPost('tourney/match/13/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Thirteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/13', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

	// Match 14
  	$edit = array();
	$edit['contestant1'] = 'user.7';
  	$this->drupalPost('tourney/match/14/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/14', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 15
	$edit = array();
	$edit['contestant1'] = 'user.4';
	$this->drupalPost('tourney/match/15/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/15', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 16
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/16');
  	$this->assertRaw(t('<tr class="odd"><td>user.9</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.25</td>'));

	// Play Sixteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/16', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.25</div>');

	// Match 17
	$edit = array();
	//Contestants
	$edit['contestant1'] = 'user.5';
	$this->drupalPost('tourney/match/17/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Seventeenth Match
	$edit = array();
	$this->drupalPost('tourney/match/17', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

	// Match 18
	$edit = array();
	$edit['contestant1'] = 'user.8';
	$this->drupalPost('tourney/match/18/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eighteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/18', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 19
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/19');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.26</td>'));

	// Play Ninteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/19', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 20
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/20');
  	$this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Twentieth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/20', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 21
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/21');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.25</td>'));

	// Play Twenty First Match
  	$edit = array();
  	$this->drupalPost('tourney/match/21', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 22
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/22');
  	$this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Second Match
  	$edit = array();
  	$this->drupalPost('tourney/match/22', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 23 
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/23');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Twenty Third Match
  	$edit = array();
  	$this->drupalPost('tourney/match/23', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 24
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/24');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Fourth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/24', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 25
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/25');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Fifth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/25', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Checking 26 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
	// End of 26 Man Tourney
    $this->pass("End of 26 Man Single Elimination Tournament ");
   }

  /**
   * Testing 27 contestants
   */
	public function testSingleElim27() {
    // Create the tournament.
   	$this->createTourney($this->controller, 27);

	// Upper Bracket
    $edit = array();
	// Match 1 Slot 1 
    $edit['contestant1'] = 'user.18';
	// Match 1 Slot 2. 
    $edit['contestant2'] = 'user.19';
	// Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
	// Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

	// Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.19</div>');

	// Match 2
	$edit = array();
	$edit['contestant1'] = 'user.10';
	$edit['contestant2'] = 'user.27';
	$this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Second Match
	$edit = array();
	$this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.10</div>');

	// Match 3
  	$edit = array();
	$edit['contestant1'] = 'user.11';
	$edit['contestant2'] = 'user.26';
  	$this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Third Match
	$edit = array();
	$this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.26</div>');

	// Match 4
  	$edit = array();
	$edit['contestant1'] = 'user.15';
	$edit['contestant2'] = 'user.22';
  	$this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourth Match
	$edit = array();
	$this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.22</div>');

	// Match 5
  	$edit = array();
	$edit['contestant1'] = 'user.14';
	$edit['contestant2'] = 'user.23';
  	$this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifth Match
	$edit = array();
	$this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 6
  	$edit = array();
	$edit['contestant1'] = 'user.17';
	$edit['contestant2'] = 'user.20';
  	$this->drupalPost('tourney/match/6/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Sixth Match
	$edit = array();
	$this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.20</div>');

	// Match 7
	$edit = array();
	$edit['contestant1'] = 'user.9';
	$edit['contestant2'] = 'user.28';
	$this->drupalPost('tourney/match/7/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Seventh Match
	$edit = array();
	$this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.9</div>');

	// Match 8
  	$edit = array();
	$edit['contestant1'] = 'user.12';
	$edit['contestant2'] = 'user.25';
  	$this->drupalPost('tourney/match/8/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eighth Match
	$edit = array();
	$this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.25</div>');

	// Match 9
  	$edit = array();
	$edit['contestant1'] = 'user.16';
	$edit['contestant2'] = 'user.21';
  	$this->drupalPost('tourney/match/9/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Ninth Match
	$edit = array();
	$this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.21</div>');

	// Match 10
	$edit = array();
	$edit['contestant1'] = 'user.8';
	$edit['contestant2'] = 'user.29';
	$this->drupalPost('tourney/match/10/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Tenth Match
	$edit = array();
	$this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');

	// Match 11
  	$edit = array();
	$edit['contestant1'] = 'user.13';
	$edit['contestant2'] = 'user.24';
  	$this->drupalPost('tourney/match/11/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eleventh Match
	$edit = array();
	$this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 12
	$edit = array();
	$edit['contestant1'] = 'user.3';
	$this->drupalPost('tourney/match/12/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Twelfth Match
	$edit = array();
	$this->drupalPost('tourney/match/12', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 13
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/13');
  	$this->assertRaw(t('<tr class="odd"><td>user.10</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.26</td>'));

	// Play Thirteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/13', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.26</div>');

	// Match 14
	$edit = array();
	$edit['contestant1'] = 'user.6';
	$this->drupalPost('tourney/match/14/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/14', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

	// Match 15
  	$edit = array();
	$edit['contestant1'] = 'user.7';
  	$this->drupalPost('tourney/match/15/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/15', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 16
	$edit = array();
	$edit['contestant1'] = 'user.4';
	$this->drupalPost('tourney/match/16/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Sixteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/16', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 17
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/17');
  	$this->assertRaw(t('<tr class="odd"><td>user.9</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.25</td>'));

	// Play Seventeenth Match 
  	$edit = array();
  	$this->drupalPost('tourney/match/17', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.25</div>');

	// Match 18
	$edit = array();
	$edit['contestant1'] = 'user.5';
	$this->drupalPost('tourney/match/18/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eighteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/18', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

	// Match 19
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/19');
  	$this->assertRaw(t('<tr class="odd"><td>user.8</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Ninteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/19', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 20
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/20');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.26</td>'));

	// Play Twentieth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/20', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 21
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/21');
  	$this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Twenty First Match
  	$edit = array();
  	$this->drupalPost('tourney/match/21', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 22
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/22');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.25</td>'));

	// Play Twenty Second Match
  	$edit = array();
  	$this->drupalPost('tourney/match/22', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 23
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/23');
  	$this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Third Match
  	$edit = array();
  	$this->drupalPost('tourney/match/23', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 24
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/24');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Twenty Fourth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/24', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 25
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/25');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Fifth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/25', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 26
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/26');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Sixth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/26', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Checking 27 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
	// End of 27 Man Tourney
    $this->pass("End of 27 Man Single Elimination Tournament ");
   }

  /**
   * Testing 28 contestants
   */
	public function testSingleElim28() {
    // Create the tournament.
   	$this->createTourney($this->controller, 28);

	// Upper Bracket 
    $edit = array();
	// Match 1 Slot 1 
    $edit['contestant1'] = 'user.18';
	// Match 1 Slot 2. 
    $edit['contestant2'] = 'user.19';
	// Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
	// Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

	// Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.19</div>');

 	// Match 2
	$edit = array();
	$edit['contestant1'] = 'user.10';
	$edit['contestant2'] = 'user.27';
	$this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Second Match
	$edit = array();
	$this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.10</div>');

	// Match 3
  	$edit = array();
	$edit['contestant1'] = 'user.11';
	$edit['contestant2'] = 'user.26';
  	$this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Third Match
	$edit = array();
	$this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.26</div>');

	// Match 4
  	$edit = array();
	$edit['contestant1'] = 'user.15';
	$edit['contestant2'] = 'user.22';
  	$this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourth Match
	$edit = array();
	$this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.22</div>');

	// Match 5
	$edit = array();
	$edit['contestant1'] = 'user.7';
	$edit['contestant2'] = 'user.30';
	$this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifth Match
	$edit = array();
	$this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.7</div>');

	// Match 6
  	$edit = array();
	$edit['contestant1'] = 'user.14';
	$edit['contestant2'] = 'user.23';
  	$this->drupalPost('tourney/match/6/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Sixth Match
	$edit = array();
	$this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 7
  	$edit = array();
	$edit['contestant1'] = 'user.17';
	$edit['contestant2'] = 'user.20';
  	$this->drupalPost('tourney/match/7/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Seventh Match
	$edit = array();
	$this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.20</div>');

	// Match 8
	$edit = array();
	$edit['contestant1'] = 'user.9';
	$edit['contestant2'] = 'user.28';
	$this->drupalPost('tourney/match/8/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eighth Match
	$edit = array();
	$this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.9</div>');

	// Match 9
  	$edit = array();
	$edit['contestant1'] = 'user.12';
	$edit['contestant2'] = 'user.25';
  	$this->drupalPost('tourney/match/9/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Ninth Match
	$edit = array();
	$this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.25</div>');

	// Match 10
  	$edit = array();
	$edit['contestant1'] = 'user.16';
	$edit['contestant2'] = 'user.21';
  	$this->drupalPost('tourney/match/10/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Tenth Match
	$edit = array();
	$this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.21</div>');

	// Match 11
	$edit = array();
	$edit['contestant1'] = 'user.8';
	$edit['contestant2'] = 'user.29';
	$this->drupalPost('tourney/match/11/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eleventh Match
	$edit = array();
	$this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');

	// Match 12
  	$edit = array();
	$edit['contestant1'] = 'user.13';
	$edit['contestant2'] = 'user.24';
  	$this->drupalPost('tourney/match/12/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Twelfth Match
	$edit = array();
	$this->drupalPost('tourney/match/12', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 13
	$edit = array();
	$edit['contestant1'] = 'user.3';
	$this->drupalPost('tourney/match/13/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Thirteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/13', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 14
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/14');
  	$this->assertRaw(t('<tr class="odd"><td>user.10</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.26</td>'));

	// Play Fourteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/14', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.26</div>');

	// Match 15
	$edit = array();
	$edit['contestant1'] = 'user.6';
	$this->drupalPost('tourney/match/15/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/15', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

	// Match 16
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/16');
  	$this->assertRaw(t('<tr class="odd"><td>user.7</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Sixteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/16', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 17
	$edit = array();
	$edit['contestant1'] = 'user.4';
	$this->drupalPost('tourney/match/17/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Seventeenth Match
	$edit = array();
	$this->drupalPost('tourney/match/17', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 18
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/18');
  	$this->assertRaw(t('<tr class="odd"><td>user.9</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.25</td>'));

	// Play Eighteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/18', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.25</div>');

	// Match 19
	$edit = array();
	$edit['contestant1'] = 'user.5';
	$this->drupalPost('tourney/match/19/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Ninteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/19', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

	// Match 20
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/20');
  	$this->assertRaw(t('<tr class="odd"><td>user.8</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twentieth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/20', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 21
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/21');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.26</td>'));

	// Play Twenty First Match
  	$edit = array();
  	$this->drupalPost('tourney/match/21', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 22
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/22');
  	$this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Twenty Second Match
  	$edit = array();
  	$this->drupalPost('tourney/match/22', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');
	
	// Match 23
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/23');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.25</td>'));

	// Play Twenty Third Match
  	$edit = array();
  	$this->drupalPost('tourney/match/23', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');
	
	// Match 24
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/24');
  	$this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Fourth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/24', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');
	
	// Match 25
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/25');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Twenty Fifth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/25', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
	
	// Match 26
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/26');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Sixth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/26', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');
	
	// Match 27
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/27');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Seventh Match
  	$edit = array();
  	$this->drupalPost('tourney/match/27', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Checking 28 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
	// End of 28 Man Tourney
    $this->pass("End of 28 Man Single Elimination Tournament ");
   }

   /**
    * Testing 29 contestants
    */
	public function testSingleElim29(){
    // Create the tournament.
   	$this->createTourney($this->controller, 29);

	// Upper Bracket
    $edit = array();
	// Match 1 Slot 1 
    $edit['contestant1'] = 'user.18';
	// Match 1 Slot 2
    $edit['contestant2'] = 'user.19';
	// Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
	// Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

	// Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.19</div>');

	// Match 2
	$edit = array();
	$edit['contestant1'] = 'user.10';
	$edit['contestant2'] = 'user.27';
	$this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Second Match
	$edit = array();
	$this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.10</div>');

	// Match 3
  	$edit = array();
	$edit['contestant1'] = 'user.11';
	$edit['contestant2'] = 'user.26';
  	$this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Third Match
	$edit = array();
	$this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.26</div>');

	// Match 4
	$edit = array();
	$edit['contestant1'] = 'user.6';
	$edit['contestant2'] = 'user.31';
	$this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourth Match
	$edit = array();
	$this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

	// Match 5
  	$edit = array();
	$edit['contestant1'] = 'user.15';
	$edit['contestant2'] = 'user.22';
  	$this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifth Match
	$edit = array();
	$this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.22</div>');

	// Match 6
	$edit = array();
	$edit['contestant1'] = 'user.7';
	$edit['contestant2'] = 'user.30';
	$this->drupalPost('tourney/match/6/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Sixth Match
	$edit = array();
	$this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.7</div>');

	// Match 7
  	$edit = array();
	$edit['contestant1'] = 'user.14';
	$edit['contestant2'] = 'user.23';
  	$this->drupalPost('tourney/match/7/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Seventh Match
	$edit = array();
	$this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 8
  	$edit = array();
	$edit['contestant1'] = 'user.17';
	$edit['contestant2'] = 'user.20';
  	$this->drupalPost('tourney/match/8/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eighth Match
	$edit = array();
	$this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.20</div>');

	// Match 9
	$edit = array();
	$edit['contestant1'] = 'user.9';
	$edit['contestant2'] = 'user.28';
	$this->drupalPost('tourney/match/9/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Ninth Match
	$edit = array();
	$this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.9</div>');

	// Match 10
  	$edit = array();
	$edit['contestant1'] = 'user.12';
	$edit['contestant2'] = 'user.25';
  	$this->drupalPost('tourney/match/10/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Tenth Match
	$edit = array();
	$this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.25</div>');

	// Match 11
  	$edit = array();
	$edit['contestant1'] = 'user.16';
	$edit['contestant2'] = 'user.21';
  	$this->drupalPost('tourney/match/11/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eleventh Match
	$edit = array();
	$this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.21</div>');

	// Match 12
	$edit = array();
	$edit['contestant1'] = 'user.8';
	$edit['contestant2'] = 'user.29';
	$this->drupalPost('tourney/match/12/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Twelfth Match
	$edit = array();
	$this->drupalPost('tourney/match/12', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');

	// Match 13
  	$edit = array();
	$edit['contestant1'] = 'user.13';
	$edit['contestant2'] = 'user.24';
  	$this->drupalPost('tourney/match/13/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Thirteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/13', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 14
	$edit = array();
	$edit['contestant1'] = 'user.3';
	$this->drupalPost('tourney/match/14/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/14', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 15
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/15');
  	$this->assertRaw(t('<tr class="odd"><td>user.10</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.26</td>'));

	// Play Fifteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/15', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.26</div>');

	// Match 16
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/16');
  	$this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.22</td>'));

	// Play Sixteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/16', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

	// Match 17
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/17');
  	$this->assertRaw(t('<tr class="odd"><td>user.7</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Seventeenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/17', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 18
	$edit = array();
	$edit['contestant1'] = 'user.4';
	$this->drupalPost('tourney/match/18/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eighteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/18', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 19
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/19');
  	$this->assertRaw(t('<tr class="odd"><td>user.9</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.25</td>'));

	// Play Ninteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/19', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.25</div>');

	// Match 20
	$edit = array();
	$edit['contestant1'] = 'user.5';
	$this->drupalPost('tourney/match/20/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Twentieth Match
	$edit = array();
	$this->drupalPost('tourney/match/20', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

	// Match 21
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/21');
  	$this->assertRaw(t('<tr class="odd"><td>user.8</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty First Match
  	$edit = array();
  	$this->drupalPost('tourney/match/21', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 22
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/22');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.26</td>'));

	// Play Twenty Second Match
  	$edit = array();
  	$this->drupalPost('tourney/match/22', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 23
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/23');
  	$this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Twenty Third Match
  	$edit = array();
  	$this->drupalPost('tourney/match/23', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 24
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/24');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.25</td>'));

	// Play Twenty Fourth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/24', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 25
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/25');
  	$this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Fifth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/25', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 26
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/26');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Twenty Sixth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/26', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 27
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/27');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Seventh Match
  	$edit = array();
  	$this->drupalPost('tourney/match/27', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 28
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/28');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Eighth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/28', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Checking 29 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
	// End of 29 Man Tourney
    $this->pass("End of 29 Man Single Elimination Tournament ");
   }

  /**
   * Testing 30 contestants
   */
	public function testSingleElim30() {
    // Create the tournament.
   	$this->createTourney($this->controller, 30);

	// Upper Bracket
    $edit = array();
	// Match 1 Slot 1 
    $edit['contestant1'] = 'user.18';
	// Match 1 Slot 2
    $edit['contestant2'] = 'user.19';
	// Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
	// Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

	// Play Second Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.19</div>');

	// Match 2
	$edit = array();
	$edit['contestant1'] = 'user.10';
	$edit['contestant2'] = 'user.27';
	$this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Second Match
	$edit = array();
	$this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.10</div>');

	// Match 3
  	$edit = array();
	$edit['contestant1'] = 'user.11';
	$edit['contestant2'] = 'user.26';
  	$this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Third Match
	$edit = array();
	$this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.26</div>');

	// Match 4
	$edit = array();
	$edit['contestant1'] = 'user.6';
	$edit['contestant2'] = 'user.31';
	$this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourth Match
	$edit = array();
	$this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

	// Match 5
  	$edit = array();
	$edit['contestant1'] = 'user.15';
	$edit['contestant2'] = 'user.22';
  	$this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifth Match
	$edit = array();
	$this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.22</div>');

	// Match 6
	$edit = array();
	$edit['contestant1'] = 'user.7';
	$edit['contestant2'] = 'user.30';
	$this->drupalPost('tourney/match/6/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Sixth Match
	$edit = array();
	$this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.7</div>');

	// Match 7
  	$edit = array();
	$edit['contestant1'] = 'user.14';
	$edit['contestant2'] = 'user.23';
  	$this->drupalPost('tourney/match/7/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Seventh Match
	$edit = array();
	$this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 8
  	$edit = array();
	$edit['contestant1'] = 'user.17';
	$edit['contestant2'] = 'user.20';
  	$this->drupalPost('tourney/match/8/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eighth Match
	$edit = array();
	$this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.20</div>');

	// Match 9
	$edit = array();
	$edit['contestant1'] = 'user.9';
	$edit['contestant2'] = 'user.28';
	$this->drupalPost('tourney/match/9/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Ninth Match
	$edit = array();
	$this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.9</div>');

	// Match 10
  	$edit = array();
	$edit['contestant1'] = 'user.12';
	$edit['contestant2'] = 'user.25';
  	$this->drupalPost('tourney/match/10/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Tenth Match
	$edit = array();
	$this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.25</div>');

	// Match 11
	$edit = array();
	$edit['contestant1'] = 'user.5';
	$edit['contestant2'] = 'user.32';
	$this->drupalPost('tourney/match/11/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eleventh Match
	$edit = array();
	$this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

	// Match 12
  	$edit = array();
	$edit['contestant1'] = 'user.16';
	$edit['contestant2'] = 'user.21';
  	$this->drupalPost('tourney/match/12/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Twelfth Match
	$edit = array();
	$this->drupalPost('tourney/match/12', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.21</div>');

	// Match 13
	$edit = array();
	$edit['contestant1'] = 'user.8';
	$edit['contestant2'] = 'user.29';
	$this->drupalPost('tourney/match/13/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Thirteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/13', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');

	// Match 14
  	$edit = array();
	$edit['contestant1'] = 'user.13';
	$edit['contestant2'] = 'user.24';
  	$this->drupalPost('tourney/match/14/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/14', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 15
	$edit = array();
	$edit['contestant1'] = 'user.3';
	$this->drupalPost('tourney/match/15/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/15', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 16
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/16');
  	$this->assertRaw(t('<tr class="odd"><td>user.10</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.26</td>'));

	// Play Sixteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/16', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.26</div>');

	// Match 17
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/17');
  	$this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.22</td>'));

	// Play Seventeenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/17', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

	// Match 18
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/18');
  	$this->assertRaw(t('<tr class="odd"><td>user.7</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Eighteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/18', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 19
	$edit = array();
	$edit['contestant1'] = 'user.4';
	$this->drupalPost('tourney/match/19/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Ninteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/19', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 20
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/20');
  	$this->assertRaw(t('<tr class="odd"><td>user.9</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.25</td>'));

	// Play Twentieth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/20', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.25</div>');

	// Match 21
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/21');
  	$this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.21</td>'));

	// Play Twenty First Match
  	$edit = array();
  	$this->drupalPost('tourney/match/21', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

	// Match 22
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/22');
  	$this->assertRaw(t('<tr class="odd"><td>user.8</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Second Match
  	$edit = array();
  	$this->drupalPost('tourney/match/22', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 23
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/23');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.26</td>'));

	// Play Twenty Third Match
  	$edit = array();
  	$this->drupalPost('tourney/match/23', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 24
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/24');
  	$this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Twenty Fourth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/24', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 25
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/25');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.25</td>'));

	// Play Twenty Fifth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/25', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 26
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/26');
  	$this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Sixth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/26', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 27
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/27');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Twenty Seventh Match
  	$edit = array();
  	$this->drupalPost('tourney/match/27', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 28
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/28');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Eighth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/28', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 29
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/29');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Ninth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/29', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Checking 30 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
	// End of 30 Man Tourney
    $this->pass("End of 30 Man Single Elimination Tournament ");
   }

  /**
   * Testing 31 contestants
   */
	public function testSingleElim31() {
    // Create the tournament.
   	$this->createTourney($this->controller, 31);
	
	// Upper Bracket
    $edit = array();
	// Match 1 Slot 1 
    $edit['contestant1'] = 'user.18';
	// Match 1 Slot 2
    $edit['contestant2'] = 'user.19';
	// Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
	// Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));

	// Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.19</div>');

	// Match 2
	$edit = array();
	$edit['contestant1'] = 'user.10';
	$edit['contestant2'] = 'user.27';
	$this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Second Match
	$edit = array();
	$this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.10</div>');

	// Match 3
  	$edit = array();
	$edit['contestant1'] = 'user.11';
	$edit['contestant2'] = 'user.26';
  	$this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Third Match
	$edit = array();
	$this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.26</div>');

	// Match 4
	$edit = array();
	$edit['contestant1'] = 'user.6';
	$edit['contestant2'] = 'user.31';
	$this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourth Match
	$edit = array();
	$this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

	// Match 5
  	$edit = array();
	$edit['contestant1'] = 'user.15';
	$edit['contestant2'] = 'user.22';
  	$this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifth Match
	$edit = array();
	$this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.22</div>');

	// Match 6
	$edit = array();
	$edit['contestant1'] = 'user.7';
	$edit['contestant2'] = 'user.30';
	$this->drupalPost('tourney/match/6/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Sixth Match
	$edit = array();
	$this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.7</div>');

	// Match 7
  	$edit = array();
	$edit['contestant1'] = 'user.14';
	$edit['contestant2'] = 'user.23';
  	$this->drupalPost('tourney/match/7/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Seventh Match
	$edit = array();
	$this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 8
	$edit = array();
	$edit['contestant1'] = 'user.4';
	$edit['contestant2'] = 'user.33';
	$this->drupalPost('tourney/match/8/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eighth Match
	$edit = array();
	$this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 9
  	$edit = array();
	$edit['contestant1'] = 'user.17';
	$edit['contestant2'] = 'user.20';
  	$this->drupalPost('tourney/match/9/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Ninth Match
	$edit = array();
	$this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.20</div>');

	// Match 10
	$edit = array();
	$edit['contestant1'] = 'user.9';
	$edit['contestant2'] = 'user.28';
	$this->drupalPost('tourney/match/10/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Tenth Match
	$edit = array();
	$this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.9</div>');

	// Match 11
  	$edit = array();
	$edit['contestant1'] = 'user.12';
	$edit['contestant2'] = 'user.25';
  	$this->drupalPost('tourney/match/11/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Eleventh Match
	$edit = array();
	$this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.25</div>');

	// Match 12
	$edit = array();
	$edit['contestant1'] = 'user.5';
	$edit['contestant2'] = 'user.32';
	$this->drupalPost('tourney/match/12/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Twelfth Match
	$edit = array();
	$this->drupalPost('tourney/match/12', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

	// Match 13
  	$edit = array();
	$edit['contestant1'] = 'user.16';
	$edit['contestant2'] = 'user.21';
  	$this->drupalPost('tourney/match/13/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Thirteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/13', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.21</div>');

	// Match 14
	$edit = array();
	$edit['contestant1'] = 'user.8';
	$edit['contestant2'] = 'user.29';
	$this->drupalPost('tourney/match/14/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fourteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/14', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');

	// Match 15
  	$edit = array();
	$edit['contestant1'] = 'user.13';
	$edit['contestant2'] = 'user.24';
  	$this->drupalPost('tourney/match/15/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Fifteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/15', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 16
	$edit = array();
	$edit['contestant1'] = 'user.3';
	$this->drupalPost('tourney/match/16/edit', $edit, t('Save'));
	//Confirm Match has been saved.
	$this->assertRaw(t('Your match has been saved'));

	// Play Sixteenth Match
	$edit = array();
	$this->drupalPost('tourney/match/16', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
	// Check to see if winner was successfully set, passing in "Players moved to next round" text.
	$this->assertRaw(t('Players moved to next round'));
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 17
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/17');
  	$this->assertRaw(t('<tr class="odd"><td>user.10</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.26</td>'));

	// Play Seventeenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/17', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.26</div>');

	// Match 18
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/18');
  	$this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.22</td>'));

	// Play Eighteenth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/18', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

	// Match 19
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/19');
  	$this->assertRaw(t('<tr class="odd"><td>user.7</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Ninteenth
  	$edit = array();
  	$this->drupalPost('tourney/match/19', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 20
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/20');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.20</td>'));

	// Play Twentieth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/20', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 21
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/21');
  	$this->assertRaw(t('<tr class="odd"><td>user.9</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.25</td>'));

	// Play Twenty First Match
  	$edit = array();
  	$this->drupalPost('tourney/match/21', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.25</div>');

	// Match 22
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/22');
  	$this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.21</td>'));

	// Play Twenty Second Match
  	$edit = array();
  	$this->drupalPost('tourney/match/22', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

	// Match 23
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/23');
  	$this->assertRaw(t('<tr class="odd"><td>user.8</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Third Match
  	$edit = array();
  	$this->drupalPost('tourney/match/23', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 24
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/24');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.26</td>'));

	// Play Twenty Fourth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/24', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 25
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/25');
  	$this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Twenty Fifth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/25', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');

	// Match 26
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/26');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.25</td>'));

	// Play Twenty Sixth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/26', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

	// Match 27
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/27');
  	$this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Seventh Match
  	$edit = array();
  	$this->drupalPost('tourney/match/27', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 28
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/28');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.23</td>'));

	// Play Twenty Eighth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/28', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Match 29
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/29');
  	$this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Twenty Ninth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/29', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');

	// Match 30
  	// Check to see if winner of previous match is being passed into this match correctly 
  	$this->drupalGet('tourney/match/30');
  	$this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
  	$this->assertRaw(t('<tr class="even"><td>user.24</td>'));

	// Play Thirtieth Match
  	$edit = array();
  	$this->drupalPost('tourney/match/30', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
  	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

	// Checking 31 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
	// Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
	// End of 31 Man Tourney
    $this->pass("End of 31 Man Single Elimination Tournament ");
   }

   /**
    * Testing 32 contestants
    */
    public function testSingleElim32() {
    // Create the tournament.
    $this->createTourney($this->controller, 32);
    
    $edit = array();
    // Match 1 Slot 1
    $edit['contestant1'] = 'user.3';
    // Match 1 Slot 2.
    $edit['contestant2'] = 'user.34';
    // Save Match 1 (/tourney/match/1)
    $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play First Match
    $edit = array();
    $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    
    // Set contestants of the Second Match.
    $edit = array();
    $edit['contestant1'] = 'user.18';
    $edit['contestant2'] = 'user.19';
    $this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Second Match
    $edit = array();
    $this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.19</div>');
    
    // Set contestants of the Third Match.
    $edit = array();
    $edit['contestant1'] = 'user.10';
    $edit['contestant2'] = 'user.27';
    $this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Third Match
    $edit = array();
    $this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.10</div>');
  
    // Set contestants of the Fourth Match.
    $edit = array();
    $edit['contestant1'] = 'user.11';
    $edit['contestant2'] = 'user.26';
    $this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Fourth Match
    $edit = array();
    $this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.26</div>');
    
    // Set contestants of the Fifth Match.
    $edit = array();
    $edit['contestant1'] = 'user.6';
    $edit['contestant2'] = 'user.31';
    $this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Fifth Match
    $edit = array();
    $this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');
    
    // Set contestants of the Sixth Match.
    $edit = array();
    $edit['contestant1'] = 'user.15';
    $edit['contestant2'] = 'user.22';
    $this->drupalPost('tourney/match/6/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Sixth Match
    $edit = array();
    $this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.22</div>');
    
    // Set contestants of the Seventh Match.
    $edit = array();
    $edit['contestant1'] = 'user.7';
    $edit['contestant2'] = 'user.30';
    $this->drupalPost('tourney/match/7/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Seventh Match
    $edit = array();
    $this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.7</div>');
    
    // Set contestants of the Eighth Match.
    $edit = array();
    $edit['contestant1'] = 'user.14';
    $edit['contestant2'] = 'user.23';
    $this->drupalPost('tourney/match/8/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Eighth Match
    $edit = array();
    $this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');
    
    // Set contestants of the Ninth Match.
    $edit = array();
    $edit['contestant1'] = 'user.4';
    $edit['contestant2'] = 'user.33';
    $this->drupalPost('tourney/match/9/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Ninth Match
    $edit = array();
    $this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');
    
    // Set contestants of the Tenth Match.
    $edit = array();
    $edit['contestant1'] = 'user.17';
    $edit['contestant2'] = 'user.20';
    $this->drupalPost('tourney/match/10/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Tenth Match
    $edit = array();
    $this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.20</div>');
  
    // Set contestants of the Eleventh Match.
    $edit = array();
    $edit['contestant1'] = 'user.9';
    $edit['contestant2'] = 'user.28';
    $this->drupalPost('tourney/match/11/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Eleventh Match
    $edit = array();
    $this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.9</div>');
    
    // Set contestants of the Twelfth Match.
    $edit = array();
    $edit['contestant1'] = 'user.12';
    $edit['contestant2'] = 'user.25';
    $this->drupalPost('tourney/match/12/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Twelfth Match
    $edit = array();
    $this->drupalPost('tourney/match/12', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.25</div>');
    
    // Set contestants of the Thriteenth Match.
    $edit = array();
    $edit['contestant1'] = 'user.5';
    $edit['contestant2'] = 'user.32';
    $this->drupalPost('tourney/match/13/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Thriteenth Match
    $edit = array();
    $this->drupalPost('tourney/match/13', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');
    
    // Set contestants of the Fourteenth Match.
    $edit = array();
    $edit['contestant1'] = 'user.16';
    $edit['contestant2'] = 'user.21';
    $this->drupalPost('tourney/match/14/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Fourteenth Match
    $edit = array();
    $this->drupalPost('tourney/match/14', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.21</div>');
    
    // Set contestants of the Fifteenth Match.
    $edit = array();
    $edit['contestant1'] = 'user.8';
    $edit['contestant2'] = 'user.29';
    $this->drupalPost('tourney/match/15/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Fifteenth Match
    $edit = array();
    $this->drupalPost('tourney/match/15', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');
    
    // Set contestants of the Sixteenth Match.
    $edit = array();
    $edit['contestant1'] = 'user.13';
    $edit['contestant2'] = 'user.24';
    $this->drupalPost('tourney/match/16/edit', $edit, t('Save'));
    // Confirm Match has been saved.
    $this->assertRaw(t('Your match has been saved'));
    
    // Play Sixteenth Match
    $edit = array();
    $this->drupalPost('tourney/match/16', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');
        
    // Match 17
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/17');
    $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.19</td>'));
    
    // Play Seventeenth Match
    $edit = array();
    $this->drupalPost('tourney/match/17', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    
    // Match 18
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/18');
    $this->assertRaw(t('<tr class="odd"><td>user.10</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.26</td>'));
    
    // Play Eighteenth Match
    $edit = array();
    $this->drupalPost('tourney/match/18', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.26</div>');
    
    // Match 19
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/19');
    $this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.22</td>'));
    
    // Play Nineteenth Match
    $edit = array();
    $this->drupalPost('tourney/match/19', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');
    
    // Match 20
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/20');
    $this->assertRaw(t('<tr class="odd"><td>user.7</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.23</td>'));
    
    // Play Twentieth Match
    $edit = array();
    $this->drupalPost('tourney/match/20', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');
    
    // Match 21
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/21');
    $this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.20</td>'));
    
    // Play Twenty First Match
    $edit = array();
    $this->drupalPost('tourney/match/21', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');
    
    // Match 22
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/22');
    $this->assertRaw(t('<tr class="odd"><td>user.9</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.25</td>'));
    
    // Play Twenty Second Match
    $edit = array();
    $this->drupalPost('tourney/match/22', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.25</div>');
    
    // Match 23
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/23');
    $this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.21</td>'));
    
    // Play Twenty Third Match
    $edit = array();
    $this->drupalPost('tourney/match/23', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');
    
    // Match 24
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/24');
    $this->assertRaw(t('<tr class="odd"><td>user.8</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.24</td>'));
    
    // Play Twenty Fourth Match
    $edit = array();
    $this->drupalPost('tourney/match/24', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');
    
    // Match 25
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/25');
    $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.26</td>'));
    
    // Play Twenty Fifth Match
    $edit = array();
    $this->drupalPost('tourney/match/25', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    
    // Match 26
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/26');
    $this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.23</td>'));
    
    // Play Twenty Sixth Match
    $edit = array();
    $this->drupalPost('tourney/match/26', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.23</div>');
    
    // Match 27
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/27');
    $this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.25</td>'));
    
    // Play Twenty Seventh Match
    $edit = array();
    $this->drupalPost('tourney/match/27', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');
    
    // Match 28
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/28');
    $this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.24</td>'));
    
    // Play Twenty Eighth Match
    $edit = array();
    $this->drupalPost('tourney/match/28', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');
    
    // Match 29
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/29');
    $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.23</td>'));
    
    // Play Twenty Ninth Match
    $edit = array();
    $this->drupalPost('tourney/match/29', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    
    // Match 30
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/30');
    $this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.24</td>'));
    
    // Play Thirtieth Match
    $edit = array();
    $this->drupalPost('tourney/match/30', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
    // Check to see if winner was successfully set, passing in "Players moved to next round" text.
    $this->assertRaw(t('Players moved to next round'));
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.24</div>');
    
    // Match 31
    // Check to see if winner of previous match is being passed into this match correctly 
    $this->drupalGet('tourney/match/31');
    $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
    $this->assertRaw(t('<tr class="even"><td>user.24</td>'));
    
    // Play Thirty First Match
    $edit = array();
    $this->drupalPost('tourney/match/31', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    
    // Checking 32 man Tourney Winner is displaying on the Tournament page
    $edit = array();
    $this->drupalGet('tourney/tournament/1');
    // Check to see if the winner is printed on the page.
    $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');
    // End of 32 Man Tourney
    $this->pass("End of 32 Man Single Elimination Tournament ");
   }
}