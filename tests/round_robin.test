<?php
class RoundRobinTestCase extends TourneyWebTestCase {
  protected $controller = 'RoundRobinController';
  
  /**
   * Test info.
   */
  public static function getInfo() {
    return array(
      'name' => t('Round Robin Tournament'),
      'description' => t('Steps through setting up a round robin tournament and playing all the way through.'),
      'group' => t('Tourney'),
    );
  }
  
  /**
   * Testing 4 contestants
   */
  public function testRoundRobin4() {
    // Create the tournament.
    $tournament_name = $this->createTourney($this->controller, 4);
    
     // ROUND 1
     // Set contestants of the First Match as same user.
     $edit = array();
     // Match 1 Slot 1
     $edit['contestant1'] = 'user.3';
     // Match 1 Slot 2
     $edit['contestant2'] = 'user.3';
     // Save Match 1
     $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
     // Confirm Tourney Module can't set the same user twice.
     $this->assertRaw(t('Contestant 1 and Contestant 2 cannot be the same person'));

     // Now change contestant 2 to a different user.
     // Match 1 Slot 2.
     $edit['contestant2'] = 'user.6';
     // Save Match 1 (/tourney/match/1)
     $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
     // Confirm Match has been saved.
     $this->assertRaw(t('Your match has been saved'));
     // Make sure the link to Tourament is present.
     $this->assertLink(t('Go to tournament !tournament', array('!tournament' => $tournament_name)));

     // Play First Match, 'contestant1' (Slot 1 or 'user.3') wins in match 1.
     $edit = array();
     $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
     // Check to see if the winner is printed on the page.
     $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

     // Set contestants Match 2
     $edit = array();
     $edit['contestant1'] = 'user.4';
     $edit['contestant2'] = 'user.5';
     // Save Match 2 (/tourney/match/2)
     $this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
     // Make sure Match has been saved.
     $this->assertRaw(t('Your match has been saved'));

     // Play Second Match
     $edit = array();
     $this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
     // Check to see if the winner is printed on the page.
     $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

     // ROUND 2
     // Match 3
     // Check to see if winner of previous match is being
     // passed into this match correctly 
     $this->drupalGet('tourney/match/3');
     $this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
     $this->assertRaw(t('<tr class="even"><td>user.3</td>'));

     // Play Third Match
     $edit = array();
     $this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
     // Check to see if the winner is printed on the page.
     $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

     // Match 4
     // Check to see if winner of previous match is being
     // passed into this match correctly 
     $this->drupalGet('tourney/match/4');
     $this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
     $this->assertRaw(t('<tr class="even"><td>user.6</td>'));

     // Play Fourth Match
     $edit = array();
     $this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
     // Check to see if the winner is printed on the page.
     $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

     // ROUND 3
     // Match 5
     // Check to see if winner of previous match is being
     // passed into this match correctly 
     $this->drupalGet('tourney/match/5');
     $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
     $this->assertRaw(t('<tr class="even"><td>user.4</td>'));

     // Play Fifth Match
     $edit = array();
     $this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
     // Check to see if the winner is printed on the page.
     $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

     // Match 6
     // Check to see if winner of previous match is being
     // passed into this match correctly 
     $this->drupalGet('tourney/match/6');
     $this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
     $this->assertRaw(t('<tr class="even"><td>user.6</td>'));

     // Play Sixth Match
     $edit = array();
     $this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
     // Check to see if the winner is printed on the page.
     $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

     // Checking 4 man Tourney Winner is displaying on the Tournament page 
     $edit = array();
     $this->drupalGet('tourney/tournament/1');
     // Check to see if the winner is printed on the page.
     $this->assertRaw('<tr class="odd"><td>user.3</td><td>3</td><td>3</td><td>0</td>');
     // End of 4 Man Tourney
     $this->pass("End of 4 Man " . $this->controller);
    }

    /**
     * Testing 6 contestants
     */
    public function testRoundRobin6() {
      // Create the tournament.
      $tournament_name = $this->createTourney($this->controller, 6);

      // ROUND 1
      $edit = array();
      // Match 1 Slot 1
      $edit['contestant1'] = 'user.3';
      // Match 1 Slot 2.
      $edit['contestant2'] = 'user.8';
      // Save Match 1 (/tourney/match/1)
      $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
      // Confirm Match has been saved.
      $this->assertRaw(t('Your match has been saved'));

      // Play First Match
      $edit = array();
      $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

      // Set contestants Match 2
      $edit = array();
      $edit['contestant1'] = 'user.4';
      $edit['contestant2'] = 'user.7';
      // Save Match 2 (/tourney/match/2)
      $this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
      // Make sure Match has been saved.
      $this->assertRaw(t('Your match has been saved'));

      // Play Second Match
      $edit = array();
      $this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.7</div>');

      // Set contestants Match 3
      $edit = array();
      $edit['contestant1'] = 'user.5';
      $edit['contestant2'] = 'user.6';
      // Save Match 3 (/tourney/match/3)
      $this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
      // Make sure Match has been saved.
      $this->assertRaw(t('Your match has been saved'));

      // Play Third Match
      $edit = array();
      $this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

      // ROUND 2
      // Match 4
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/4');
      $this->assertRaw(t('<tr class="odd"><td>user.7</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.3</td>'));

      // Play Fourth Match
      $edit = array();
      $this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

      // Match 5
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/5');
      $this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.8</td>'));

      // Play Fifth Match
      $edit = array();
      $this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

      // Match 6
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/6');
      $this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.4</td>'));

      // Play Sixth Match
      $edit = array();
      $this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

      // ROUND 3
      // Match 7
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/7');
      $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.6</td>'));

      // Play Seventh Match
      $edit = array();
      $this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

      // Match 8
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/8');
      $this->assertRaw(t('<tr class="odd"><td>user.7</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.5</td>'));

      // Play Eighth Match
      $edit = array();
      $this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

      // Match 9
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/9');
      $this->assertRaw(t('<tr class="odd"><td>user.8</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.4</td>'));

      // Play Ninth Match
      $edit = array();
      $this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

      // ROUND 4
      // Match 10
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/10');
      $this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.3</td>'));

      // Play Tenth Match
      $edit = array();
      $this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

      // Match 11
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/11');
      $this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.6</td>'));

      // Play Eleventh Match
      $edit = array();
      $this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

      // Match 12
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/12');
      $this->assertRaw(t('<tr class="odd"><td>user.8</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.7</td>'));

      // Play Twelfth Match
      $edit = array();
      $this->drupalPost('tourney/match/12', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');

      // ROUND 5
      // Match 13
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/13');
      $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.4</td>'));

      // Play Seventeenth Match
      $edit = array();
      $this->drupalPost('tourney/match/13', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

      // Match 14
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/14');
      $this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.8</td>'));

      // Play Fourteenth Match
      $edit = array();
      $this->drupalPost('tourney/match/14', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');

      // Match 15
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/15');
      $this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.7</td>'));

      // Play Fifteenth Match
      $edit = array();
      $this->drupalPost('tourney/match/15', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.7</div>');
      
      // Checking 6 man Tourney Winner is displaying on the Tournament page 
      $edit = array();
      $this->drupalGet('tourney/tournament/1');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<tr class="odd"><td>user.3</td><td>5</td><td>5</td><td>0</td>');
      // End of 6 Man Tourney
      $this->pass("End of 6 Man " . $this->controller);
      }

    /**
     * Testing 8 contestants
     */
    public function testRoundRobin8() {
      // Create the tournament.
      $tournament_name = $this->createTourney($this->controller, 8);

      // ROUND 1
      $edit = array();
      // Match 1 Slot 1
      $edit['contestant1'] = 'user.3';
      // Match 1 Slot 2.
      $edit['contestant2'] = 'user.10';
      // Save Match 1 (/tourney/match/1)
      $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
      // Confirm Match has been saved.
      $this->assertRaw(t('Your match has been saved'));

      // Play First Match
      $edit = array();
      $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

      // Set contestants Match 2
      $edit = array();
      $edit['contestant1'] = 'user.4';
      $edit['contestant2'] = 'user.9';
      // Save Match 2 (/tourney/match/2)
      $this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
      // Make sure Match has been saved.
      $this->assertRaw(t('Your match has been saved'));

      // Play Second Match
      $edit = array();
      $this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.9</div>');

      // Set contestants Match 3
      $edit = array();
      $edit['contestant1'] = 'user.5';
      $edit['contestant2'] = 'user.8';
      // Save Match 3 (/tourney/match/3)
      $this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
      // Make sure Match has been saved.
      $this->assertRaw(t('Your match has been saved'));

      // Play Third Match
      $edit = array();
      $this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');

      // Set contestants Match 4
      $edit = array();
      $edit['contestant1'] = 'user.6';
      $edit['contestant2'] = 'user.7';
      // Save Match 4 (/tourney/match/4)
      $this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
      // Make sure Match has been saved.
      $this->assertRaw(t('Your match has been saved'));

      // Play Fourth Match
      $edit = array();
      $this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.7</div>');

      // ROUND 2
      // Match 5
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/5');
      $this->assertRaw(t('<tr class="odd"><td>user.9</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.3</td>'));

      // Play Fifth Match
      $edit = array();
      $this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

      // Match 6
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/6');
      $this->assertRaw(t('<tr class="odd"><td>user.8</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.10</td>'));

      // Play Sixth Match
      $edit = array();
      $this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');

      // Match 7
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/7');
      $this->assertRaw(t('<tr class="odd"><td>user.7</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.4</td>'));

      // Play Seventh Match
      $edit = array();
      $this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.7</div>');

      // Match 8
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/8');
      $this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.5</td>'));

      // Play Eighth Match
      $edit = array();
      $this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

      // ROUND 3
      // Match 9
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/9');
      $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.8</td>'));

      // Play Ninth Match
      $edit = array();
      $this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

      // Match 10
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/10');
      $this->assertRaw(t('<tr class="odd"><td>user.9</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.7</td>'));

      // Play Tenth Match
      $edit = array();
      $this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.7</div>');

      // Match 11
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/11');
      $this->assertRaw(t('<tr class="odd"><td>user.10</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.6</td>'));

      // Play Eleventh Match
      $edit = array();
      $this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

      // Match 12
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/12');
      $this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.5</td>'));

      // Play Twelfth Match
      $edit = array();
      $this->drupalPost('tourney/match/12', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

      // ROUND 4
      // Match 13
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/13');
      $this->assertRaw(t('<tr class="odd"><td>user.7</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.3</td>'));

      // Play Thriteenth Match
      $edit = array();
      $this->drupalPost('tourney/match/13', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

      // Match 14
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/14');
      $this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.8</td>'));

      // Play Fourteenth Match
      $edit = array();
      $this->drupalPost('tourney/match/14', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

      // Match 15
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/15');
      $this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.9</td>'));

      // Play Fifteenth Match
      $edit = array();
      $this->drupalPost('tourney/match/15', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

      // Match 16
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/16');
      $this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.10</td>'));

      // Play Sixteenth Match
      $edit = array();
      $this->drupalPost('tourney/match/16', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

      // ROUND 5
      // Match 17
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/17');
      $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.6</td>'));

      // Play Seventeenth Match
      $edit = array();
      $this->drupalPost('tourney/match/17', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

      // Match 18
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/18');
      $this->assertRaw(t('<tr class="odd"><td>user.7</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.5</td>'));

      // Play Eighteenth Match
      $edit = array();
      $this->drupalPost('tourney/match/18', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

      // Match 19
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/19');
      $this->assertRaw(t('<tr class="odd"><td>user.8</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.4</td>'));

      // Play Ninteenth  Match
      $edit = array();
      $this->drupalPost('tourney/match/19', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

      // Match 20
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/20');
      $this->assertRaw(t('<tr class="odd"><td>user.9</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.10</td>'));

      // Play Twentieth Match
      $edit = array();
      $this->drupalPost('tourney/match/20', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.10</div>');

      // ROUND 6
      // Match 21
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/21');
      $this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.3</td>'));

      // Play Twenty First Match
      $edit = array();
      $this->drupalPost('tourney/match/21', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

      // Match 22
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/22');
      $this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.6</td>'));

      // Play Twenty Second Match
      $edit = array();
      $this->drupalPost('tourney/match/22', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

      // Match 23
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/23');
      $this->assertRaw(t('<tr class="odd"><td>user.10</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.7</td>'));

      // Play Twenty Third Match
      $edit = array();
      $this->drupalPost('tourney/match/23', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.10</div>');

      // Match 24
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/24');
      $this->assertRaw(t('<tr class="odd"><td>user.9</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.8</td>'));

      // Play Twenty Fourth Match
      $edit = array();
      $this->drupalPost('tourney/match/24', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.9</div>');

      // ROUND 7
      // Match 25
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/25');
      $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.4</td>'));

      // Play Twenty Fifth Match
      $edit = array();
      $this->drupalPost('tourney/match/25', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

      // Match 26
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/26');
      $this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.10</td>'));

      // Play Twenty Sixth Match
      $edit = array();
      $this->drupalPost('tourney/match/26', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.10</div>');

      // Match 27
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/27');
      $this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.9</td>'));

      // Play Twenty Seventh Match
      $edit = array();
      $this->drupalPost('tourney/match/27', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.9</div>');

      // Match 28
      // Check to see if winner of previous match is being
      // passed into this match correctly 
      $this->drupalGet('tourney/match/28');
      $this->assertRaw(t('<tr class="odd"><td>user.7</td>'));
      $this->assertRaw(t('<tr class="even"><td>user.8</td>'));

      // Play Twenty Eighth Match
      $edit = array();
      $this->drupalPost('tourney/match/28', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');

      // Checking 8 man Tourney Winner is displaying on the Tournament page 
      $edit = array();
      $this->drupalGet('tourney/tournament/1');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<tr class="odd"><td>user.3</td><td>7</td><td>7</td><td>0</td>');
      // End of 8 Man Tourney
      $this->pass("End of 8 Man " . $this->controller);
      }


    /**
     * Testing 10 contestants
     */
    public function testRoundRobin10() {
      // Create the tournament.
      $tournament_name = $this->createTourney($this->controller, 10);

      // ROUND 1
      $edit = array();
      // Match 1 Slot 1
      $edit['contestant1'] = 'user.3';
      // Match 1 Slot 2.
      $edit['contestant2'] = 'user.12';
      // Save Match 1 (/tourney/match/1)
      $this->drupalPost('tourney/match/1/edit', $edit, t('Save'));
      // Confirm Match has been saved.
      $this->assertRaw(t('Your match has been saved'));

      // Play First Match
      $edit = array();
      $this->drupalPost('tourney/match/1', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
      // Check to see if the winner is printed on the page.
      $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

       // Set contestants Match 2
       $edit = array();
       $edit['contestant1'] = 'user.4';
       $edit['contestant2'] = 'user.11';
       // Save Match 2 (/tourney/match/2)
       $this->drupalPost('tourney/match/2/edit', $edit, t('Save'));
       // Make sure Match has been saved.
       $this->assertRaw(t('Your match has been saved'));

       // Play Second Match
       $edit = array();
       $this->drupalPost('tourney/match/2', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.11</div>');
       
       // Set contestants Match 3
       $edit = array();
       $edit['contestant1'] = 'user.5';
       $edit['contestant2'] = 'user.10';
       // Save Match 3 (/tourney/match/3)
       $this->drupalPost('tourney/match/3/edit', $edit, t('Save'));
       // Make sure Match has been saved.
       $this->assertRaw(t('Your match has been saved'));

       // Play Third Match
       $edit = array();
       $this->drupalPost('tourney/match/3', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.10</div>');
       
       // Set contestants Match 4
       $edit = array();
       $edit['contestant1'] = 'user.6';
       $edit['contestant2'] = 'user.9';
       // Save Match 4 (/tourney/match/4)
       $this->drupalPost('tourney/match/4/edit', $edit, t('Save'));
       // Make sure Match has been saved.
       $this->assertRaw(t('Your match has been saved'));

       // Play Fourth Match
       $edit = array();
       $this->drupalPost('tourney/match/4', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.9</div>');
       
       // Set contestants Match 5
       $edit = array();
       $edit['contestant1'] = 'user.7';
       $edit['contestant2'] = 'user.8';
       // Save Match 5 (/tourney/match/5)
       $this->drupalPost('tourney/match/5/edit', $edit, t('Save'));
       // Make sure Match has been saved.
       $this->assertRaw(t('Your match has been saved'));

       // Play Fifth Match
       $edit = array();
       $this->drupalPost('tourney/match/5', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');

       // ROUND 2
       // Match 6
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/6');
       $this->assertRaw(t('<tr class="odd"><td>user.11</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.3</td>'));

       // Play Sixth Match
       $edit = array();
       $this->drupalPost('tourney/match/6', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

       // Match 7
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/7');
       $this->assertRaw(t('<tr class="odd"><td>user.10</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.12</td>'));

       // Play Seventh Match
       $edit = array();
       $this->drupalPost('tourney/match/7', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.10</div>');

       // Match 8
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/8');
       $this->assertRaw(t('<tr class="odd"><td>user.9</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.4</td>'));

       // Play Eighth Match
       $edit = array();
       $this->drupalPost('tourney/match/8', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.9</div>');

       // Match 9
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/9');
       $this->assertRaw(t('<tr class="odd"><td>user.8</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.5</td>'));

       // Play Ninth Match
       $edit = array();
       $this->drupalPost('tourney/match/9', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');

       // Match 10
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/10');
       $this->assertRaw(t('<tr class="odd"><td>user.7</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.6</td>'));

       // Play Tenth Match
       $edit = array();
       $this->drupalPost('tourney/match/10', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.7</div>');

       // ROUND 3
       // Match 11
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/11');
       $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.10</td>'));

       // Play Eleventh Match
       $edit = array();
       $this->drupalPost('tourney/match/11', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

       // Match 12
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/12');
       $this->assertRaw(t('<tr class="odd"><td>user.11</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.9</td>'));

       // Play Twelfth Match
       $edit = array();
       $this->drupalPost('tourney/match/12', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.9</div>');

       // Match 13
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/13');
       $this->assertRaw(t('<tr class="odd"><td>user.12</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.8</td>'));

       // Play Thirteenth Match
       $edit = array();
       $this->drupalPost('tourney/match/13', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');

       // Match 14
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/14');
       $this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.7</td>'));

       // Play Fourteenth Match
       $edit = array();
       $this->drupalPost('tourney/match/14', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.7</div>');

       // Match 15
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/15');
       $this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.6</td>'));

       // Play Fifteenth Match
       $edit = array();
       $this->drupalPost('tourney/match/15', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

       // ROUND 4
       // Match 16
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/16');
       $this->assertRaw(t('<tr class="odd"><td>user.9</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.3</td>'));

       // Play Sixteenth Match
       $edit = array();
       $this->drupalPost('tourney/match/16', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

       // Match 17
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/17');
       $this->assertRaw(t('<tr class="odd"><td>user.8</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.10</td>'));

       // Play Seventeenth Match
       $edit = array();
       $this->drupalPost('tourney/match/17', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.8</div>');

       // Match 18
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/18');
       $this->assertRaw(t('<tr class="odd"><td>user.7</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.11</td>'));

       // Play Eighteenth Match
       $edit = array();
       $this->drupalPost('tourney/match/18', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.7</div>');

       // Match 19
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/19');
       $this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.12</td>'));

       // Play Ninteenth Match
       $edit = array();
       $this->drupalPost('tourney/match/19', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

       // Match 20
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/20');
       $this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.4</td>'));

       // Play Twentieth Match
       $edit = array();
       $this->drupalPost('tourney/match/20', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

       // ROUND 5
       // Match 21
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/21');
       $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.8</td>'));

       // Play Twenty First Match
       $edit = array();
       $this->drupalPost('tourney/match/21', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

       // Match 22
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/22');
       $this->assertRaw(t('<tr class="odd"><td>user.9</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.7</td>'));

       // Play Twenty Second Match
       $edit = array();
       $this->drupalPost('tourney/match/22', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.7</div>');

       // Match 23
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/23');
       $this->assertRaw(t('<tr class="odd"><td>user.10</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.6</td>'));

       // Play Twenty Third Match
       $edit = array();
       $this->drupalPost('tourney/match/23', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

       // Match 24
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/24');
       $this->assertRaw(t('<tr class="odd"><td>user.11</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.5</td>'));

       // Play Twenty Fourth Match
       $edit = array();
       $this->drupalPost('tourney/match/24', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

       // Match 25
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/25');
       $this->assertRaw(t('<tr class="odd"><td>user.12</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.4</td>'));

       // Play Twenty Fifth Match
       $edit = array();
       $this->drupalPost('tourney/match/25', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');
       
       // ROUND 6
       // Match 26
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/26');
       $this->assertRaw(t('<tr class="odd"><td>user.7</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.3</td>'));

       // Play Twenty Sixth Match
       $edit = array();
       $this->drupalPost('tourney/match/26', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

       // Match 27
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/27');
       $this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.8</td>'));

       // Play Twenty Seventh Match
       $edit = array();
       $this->drupalPost('tourney/match/27', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.6</div>');

       // Match 28
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/28');
       $this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.9</td>'));

       // Play Twenty Eighth Match
       $edit = array();
       $this->drupalPost('tourney/match/28', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

       // Match 29
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/29');
       $this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.10</td>'));

       // Play Twenty Ninth Match
       $edit = array();
       $this->drupalPost('tourney/match/29', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

       // Match 30
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/30');
       $this->assertRaw(t('<tr class="odd"><td>user.12</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.11</td>'));

       // Play Thirtieth Match
       $edit = array();
       $this->drupalPost('tourney/match/30', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.12</div>');

       // ROUND 7
       // Match 31
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/31');
       $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.6</td>'));

       // Play Thirty First Match
       $edit = array();
       $this->drupalPost('tourney/match/31', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

       // Match 32
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/32');
       $this->assertRaw(t('<tr class="odd"><td>user.7</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.5</td>'));

       // Play Thirty Second Match
       $edit = array();
       $this->drupalPost('tourney/match/32', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.5</div>');

       // Match 33
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/13');
       $this->assertRaw(t('<tr class="odd"><td>user.12</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.8</td>'));

       // Play Thirty Third Match
       $edit = array();
       $this->drupalPost('tourney/match/33', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

       // Match 34
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/34');
       $this->assertRaw(t('<tr class="odd"><td>user.9</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.12</td>'));

       // Play Thirty Fourth Match
       $edit = array();
       $this->drupalPost('tourney/match/34', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.12</div>');

       // Match 35
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/35');
       $this->assertRaw(t('<tr class="odd"><td>user.10</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.11</td>'));

       // Play Thirty Fifth Match
       $edit = array();
       $this->drupalPost('tourney/match/35', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.11</div>');
       
       // ROUND 8
       // Match 36
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/36');
       $this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.3</td>'));

       // Play Thirty Sixth Match
       $edit = array();
       $this->drupalPost('tourney/match/36', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

       // Match 37
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/37');
       $this->assertRaw(t('<tr class="odd"><td>user.4</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.6</td>'));

       // Play Thirty Seventh Match
       $edit = array();
       $this->drupalPost('tourney/match/37', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.4</div>');

       // Match 38
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/38');
       $this->assertRaw(t('<tr class="odd"><td>user.12</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.7</td>'));

       // Play Thirty Eighth Match
       $edit = array();
       $this->drupalPost('tourney/match/38', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.12</div>');

       // Match 39
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/39');
       $this->assertRaw(t('<tr class="odd"><td>user.11</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.8</td>'));

       // Play Thirty Ninth Match
       $edit = array();
       $this->drupalPost('tourney/match/39', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.11</div>');

       // Match 40
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/40');
       $this->assertRaw(t('<tr class="odd"><td>user.10</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.9</td>'));

       // Play Fourtieth Match
       $edit = array();
       $this->drupalPost('tourney/match/40', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.10</div>');

       // ROUND 9
       // Match 41
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/41');
       $this->assertRaw(t('<tr class="odd"><td>user.3</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.4</td>'));

       // Play Forty First Match
       $edit = array();
       $this->drupalPost('tourney/match/41', $edit, t('Win'), array(), array(), 'tourney-game-win-form');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.3</div>');

       // Match 42
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/42');
       $this->assertRaw(t('<tr class="odd"><td>user.5</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.12</td>'));

       // Play Forty Second Match
       $edit = array();
       $this->drupalPost('tourney/match/42', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.12</div>');

       // Match 43
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/43');
       $this->assertRaw(t('<tr class="odd"><td>user.6</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.11</td>'));

       // Play Forty Third Match
       $edit = array();
       $this->drupalPost('tourney/match/43', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.11</div>');

       // Match 44
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/44');
       $this->assertRaw(t('<tr class="odd"><td>user.7</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.10</td>'));

       // Play Forty Fourth Match
       $edit = array();
       $this->drupalPost('tourney/match/44', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.10</div>');

       // Match 45
       // Check to see if winner of previous match is being
       // passed into this match correctly 
       $this->drupalGet('tourney/match/45');
       $this->assertRaw(t('<tr class="odd"><td>user.8</td>'));
       $this->assertRaw(t('<tr class="even"><td>user.9</td>'));

       // Play Forty Fifth Match
       $edit = array();
       $this->drupalPost('tourney/match/45', $edit, t('Win'), array(), array(), 'tourney-game-win-form--2');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<div class="field-label">Winner:</div><div class="field-content">user.9</div>');
       

       // Checking 10 man Tourney Winner is displaying on the Tournament page 
       $edit = array();
       $this->drupalGet('tourney/tournament/1');
       // Check to see if the winner is printed on the page.
       $this->assertRaw('<tr class="odd"><td>user.3</td><td>9</td><td>9</td><td>0</td>');
       // End of 10 Man Tourney
       $this->pass("End of 10 Man " . $this->controller);
      }
}